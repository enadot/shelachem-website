---
name: שלכם — מימוש זכויות רפואיות
description: אתר לידים בעברית (RTL) שהופך בירוקרטיה רפואית לשפה של בני אדם
colors:
  brand: "#0000ff"
  brand-hover: "#0000c9"
  brand-deep: "#122680"
  banner: "#122680"
  ink: "#0d253d"
  ink-secondary: "#273951"
  ink-muted: "#4a5b74"
  ink-faint: "#616f88"
  surface: "#f6f9fc"
  surface-blue: "#eef0ff"
  hairline: "#e3e8ee"
  hairline-soft: "#eef1f6"
  accent: "#d93a38"
  accent-hover: "#c22f2d"
  accent-tint: "#fdf2f2"
  accent-tint-strong: "#fde3e2"
  accent-text: "#c93330"
typography:
  display:
    fontFamily: "RaananPro, SimplerPro, system-ui, sans-serif"
    fontSize: "clamp(40px, 4.45vw, 64px)"
    fontWeight: 300
    lineHeight: 1.08
    letterSpacing: "-0.025em"
  headline:
    fontFamily: "RaananPro, SimplerPro, system-ui, sans-serif"
    fontSize: "clamp(26px, 3vw, 36px)"
    fontWeight: 300
    lineHeight: 1.15
    letterSpacing: "-0.025em"
  title:
    fontFamily: "SimplerPro, system-ui, sans-serif"
    fontSize: "21px"
    fontWeight: 700
    lineHeight: 1.3
  body:
    fontFamily: "SimplerPro, system-ui, sans-serif"
    fontSize: "16.5px"
    fontWeight: 400
    lineHeight: 1.625
  body-read:
    fontFamily: "SimplerPro, system-ui, sans-serif"
    fontSize: "18px"
    fontWeight: 400
    lineHeight: 1.75
  label:
    fontFamily: "SimplerPro, system-ui, sans-serif"
    fontSize: "15px"
    fontWeight: 700
    lineHeight: 1.2
rounded:
  input: "10px"
  card-sm: "12px"
  card-md: "16px"
  card: "18px"
  card-hero: "20px"
  pill: "9999px"
spacing:
  gutter-mobile: "24px"
  gutter-desktop: "clamp(24px, 6.7vw, 96px)"
  section-y: "48px"
  section-y-desktop: "64px"
  container: "1240px"
  container-read: "1040px"
components:
  button-brand:
    backgroundColor: "{colors.brand}"
    textColor: "#ffffff"
    rounded: "{rounded.pill}"
    padding: "12px 28px"
    height: "50px"
    typography: "{typography.label}"
  button-brand-hover:
    backgroundColor: "{colors.brand-hover}"
  button-accent:
    backgroundColor: "{colors.accent}"
    textColor: "#ffffff"
    rounded: "{rounded.pill}"
    padding: "12px 28px"
  button-accent-hover:
    backgroundColor: "{colors.accent-hover}"
  button-outline:
    backgroundColor: "#ffffff"
    textColor: "{colors.ink}"
    rounded: "{rounded.pill}"
    padding: "12px 28px"
  button-outline-hover:
    textColor: "{colors.brand}"
  input-field:
    backgroundColor: "#ffffff"
    textColor: "{colors.ink}"
    rounded: "{rounded.input}"
    padding: "12px 16px"
    height: "48px"
  card-base:
    backgroundColor: "#ffffff"
    textColor: "{colors.ink}"
    rounded: "{rounded.card-sm}"
    padding: "24px"
  card-quiet:
    backgroundColor: "{colors.surface}"
    textColor: "{colors.ink-secondary}"
    rounded: "{rounded.card-md}"
    padding: "24px"
  chip-brand:
    backgroundColor: "{colors.surface-blue}"
    textColor: "{colors.brand}"
    rounded: "{rounded.pill}"
    padding: "6px 16px"
  chip-accent:
    backgroundColor: "{colors.accent-tint}"
    textColor: "{colors.accent-text}"
    rounded: "{rounded.pill}"
    padding: "4px 12px"
  banner-navy:
    backgroundColor: "{colors.banner}"
    textColor: "#ffffff"
    rounded: "{rounded.card}"
    padding: "40px 32px"
---

# Design System: שלכם

## Overview

**Creative North Star: "הפקיד שבצד שלכם"**

המערכת הזאת לוקחת את אוצר המילים החזותי של הבירוקרטיה הישראלית — כחול טופס, מספרים
מיושרים, שדות מוקפדים, פנייה רשמית — ומסיטה אותו 180 מעלות: אותה סמכות, רק שהפעם היא
עובדת בשבילכם. הכחול (`#0000ff`) אינו כחול "מוצר" מרוכך; זהו הכחול הטהור של טופס
ממשלתי, והוא נשאר טהור בכוונה. מה שנוסף עליו הוא **האדום** — לא אדום של שגיאה אלא של
עט שמסמן, בתוך מסמך צפוף, את המילה שבאמת חשובה.

הצפיפות בינונית ונדיבה כלפי מטה: הרבה אוויר בין קבוצות, מעט מאוד אוויר בתוך קבוצה.
משטחים שטוחים על נייר קריר (`#f6f9fc`), קווי שיער דקים במקום צללים, וצל אחד גדול ורך
שנשמר אך ורק לכרטיס הטופס — האובייקט היחיד באתר שמותר לו לצוף. הטיפוגרפיה עברית
ראשונה: RaananPro במשקל 300 נושאת את הכותרות בגדלים גדולים, ומשקל 900 בתוך אותה שורה
נושא את מילת המפתח. ההיפוך בין 300 ל-900 באותה כותרת הוא החתימה של המותג.

האנטי-רפרנס מוצהר: זו אינה קטגוריית "משרד עורכי דין" — בלי זהב, בלי סריפים
קלאסיציסטיים, בלי דימויי סמכות. גם לא קטגוריית SaaS — בלי גרדיאנטים סגולים, בלי
זכוכית, בלי איורים אקסונומטריים. הקהל נמצא במצוקה רפואית ומוקף ממילא בטפסים; האתר
מוכר שקט, בהירות וכתובת אחת ברורה לפנות אליה.

**Key Characteristics:**
- כחול טופס טהור `#0000ff` כקול הראשי, ללא ריכוך
- ניגוד משקלים 300↔900 בתוך כותרת אחת, עם קו אדום עבה מתחת למילת המפתח
- משטחים שטוחים + קווי שיער; צל דרמטי אחד, רק לכרטיס הטופס
- מספרים תמיד ב-`font-feature-settings: "tnum"` — יציבות של דוח, לא של שיווק
- RTL אמיתי: כל המרווחים הלוגיים (`ps/pe/ms/me`), לא `left/right`
- כל מסלול נגמר בפעולה אחת: בדיקת זכאות

## Colors

פלטה דו-קולית קפדנית: כחול בירוקרטי לסמכות ולניווט, אדום סימון לנקודות ההכרעה — והכל
יושב על סקאלת דיו כחלחלה אחת, כך שאין שום אפור נייטרלי באתר.

### Primary
- **כחול טופס** (`#0000ff`): הקול הראשי. כפתורי הפעולה הראשית, קישורים, מספרי מדדים,
  טבעת הפוקוס וחצי ההירו. כחול טהור לחלוטין ולא מרוכך — הבחירה הזאת היא הצהרה, לא
  ברירת מחדל. עובר AA מול לבן (8.6:1) ולכן משמש גם לטקסט קטן.
- **כחול טופס — hover** (`#0000c9`): מעבר על כפתורים כחולים. כיהוי, לא שינוי גוון.
- **נייבי מסמך** (`#122680`): משטחי הבאנר — הירו של עמודי משנה, רצועות מדדים,
  CTA באמצע כתבה. הכחול כשהוא הופך לרקע ולא לפעולה.

### Secondary
- **אדום סימון** (`#d93a38`): לא אדום שגיאה. זהו העט שמסמן את מה שחשוב — הקו התחתון
  העבה מתחת למילת המפתח בכותרות, עיגול הטלפון בהדר, מדד ה-"0 ₪". הגוון הזה נבחר
  במדויק כדי לעבור AA מול טקסט לבן (4.55:1) במקום המקור (`#f0514f`) שנפל ב-3.5:1.
- **אדום סימון — hover** (`#c22f2d`)
- **אדום טקסט** (`#c93330`): הווריאנט לטקסט אדום על רקע בהיר, כשהאדום צריך להיקרא ולא
  לצעוק.
- **גוון אדום** (`#fdf2f2`) / **גוון אדום חזק** (`#fde3e2`): רקעי תגיות ומספרי שלבים.

### Neutral
- **דיו כהה** (`#0d253d`): כל הכותרות וטקסט מודגש. כחלחל, לא שחור.
- **דיו משני** (`#273951`): פסקאות גוף — ברירת המחדל לקריאה ארוכה.
- **דיו עמום** (`#4a5b74`): קישורי פוטר, טקסט עזר.
- **דיו חלוש** (`#616f88`): מטא-דאטה, פירורי לחם, placeholder. **הערך הזה הוא רצפה
  ולא הצעה** — 4.8:1 מול `#f6f9fc`; כל הבהרה שלו שוברת AA.
- **נייר קריר** (`#f6f9fc`): רקע הסקשן החלופי. הכהות היחידה שמפרידה סקשנים.
- **תכלת טופס** (`#eef0ff`): רקע תגיות כחולות ומספרי שלבים.
- **קו שיער** (`#e3e8ee`) / **קו שיער רך** (`#eef1f6`): כל הגבולות באתר.

### Named Rules

**חוק הקו האדום.** האדום מסמן, לא ממלא. הוא מופיע כקו תחתון עבה (4–7px), כתגית קטנה,
או כעיגול טלפון — לעולם לא כרקע של סקשן שלם ולא כשטח גדול. אם האדום תופס יותר מ-10%
מהמסך, הסימון הפסיק להיות סימון.

**חוק אפס-אפור.** אין באתר אף אפור נייטרלי. כל טקסט משני, כל גבול וכל רקע נגזרים
מסקאלת הדיו הכחלחלה. `#64748b` ודומיו אסורים — הם שוברים גם את הגוון וגם את הניגודיות.

**חוק הכחול הטהור.** `#0000ff` לא מתרכך ל-`#2563eb` ולא לשום "כחול מוצר" נוח. הטהרה
היא הזהות.

## Typography

**Display Font:** RaananPro (fallback: SimplerPro, system-ui, sans-serif)
**Body Font:** SimplerPro (fallback: system-ui, sans-serif)

שני הפונטים עבריים ומוגשים מקומית כ-woff2 עם `font-display: swap`. RaananPro הוא
פונט תצוגה עם קונטרסט גבוה בין המשקלים — 300 שלו אוורירי ואצילי, 900 שלו כמעט בלוקי,
וההפרש הזה הוא בדיוק מה שהמערכת מנצלת. SimplerPro הוא ניטרלי, פונקציונלי ונקי — הוא
לא מנסה להיות אישיות, הוא מנסה להיות קריא לבן 58 על אנדרואיד ישן.

**Character:** כותרת מדברת בשקט וקוראת בקול רק את המילה האחת שחשובה. גוף הטקסט מדבר
עברית של בני אדם, לא של פקידים.

### Hierarchy
- **Display** (300, `clamp(40px, 4.45vw, 64px)`, lh 1.08, tracking דחוס): h1 של דף
  הבית בלבד. מילת המפתח בתוכו במשקל 900 עם קו אדום.
- **Headline** (300, `clamp(26px, 3vw, 36px)`, lh 1.15): h2 של סקשנים ו-h1 של עמודי
  משנה (38–54px). אותו דקדוק: קל + מודגש.
- **Title** (700, 21px): h3 בתוך תוכן, כותרות כרטיסים (18px).
- **Body** (400, 16.5–17px, lh 1.625): פסקאות בעמודי שירות/מוסד, תיאורי כרטיסים.
- **Body–Read** (400, 18px, lh 1.75): גוף כתבה במגזין. מדידה מוגבלת ל-1040px מכל.
- **Label** (700, 15px): תגיות, כפתורים, מטא-דאטה. 13–14.5px למטא-דאטה שניונית.

### Named Rules

**חוק 300/900.** כותרת אינה מודגשת כולה ואינה קלה כולה. היא נכתבת ב-300 ומילת המפתח
שלה — ורק היא — נושאת 900 וקו אדום. `SectionHeading` ו-`.keyword-underline` הם
המימוש; אין לשחזר את התבנית ידנית.

**חוק ה-tnum.** כל מספר שמופיע כנתון — סכומים, אחוזי נכות, טלפונים, מספרי שלבים,
מדדים — מקבל `.tnum`. מספרים שרוקדים ברוחב הם ההבדל בין דוח לבין באנר.

**חוק המדידה.** גוף טקסט לא עובר 75 תווים לשורה. במגזין המכל הוא 1040px; בעמודי
שירות פסקאות מוגבלות ל-760–820px.

**איסור eyebrow.** אין תגית/קיקר מעל כותרת. הכותרת נושאת את עצמה.

## Layout

מכל ראשי `1240px` לעמודי מוצר ו-`1040px` לעמודי קריאה, שניהם ממורכזים. שולי הצד הם
`24px` במובייל ו-`clamp(24px, 6.7vw, 96px)` בדסקטופ — כלומר השוליים גדלים עם המסך
במקום להיתקע. מקצב אנכי: `py-48px` במובייל, `py-64px` בדסקטופ, לכל סקשן.

הפרדת סקשנים נעשית בשתי דרכים בלבד: החלפת רקע לבן↔`#f6f9fc`, או רצועת נייבי. אין
קווים מפרידים בין סקשנים.

הגריד הדומיננטי בעמודי תוכן הוא `[1fr 380px]` (שירות) או `[1fr 260px]` (כתבה) — עמוד
ראשי + סיידבר דביק (`top: 96px`) שנקרס לזרימה אחת מתחת ל-768px. עמודי אינדקס עובדים
ב-1→2→3/4 עמודות ב-`sm`/`md`.

נקודות שבירה: `sm` 640px, `md` 768px (השבירה האמיתית — כאן משתנה הניווט, ההירו
והסיידברים), `lg` 1024px.

RTL הוא ברירת המחדל של המסמך (`dir="rtl"` על `<html>`), ולכן **כל** מרווח כיווני
נכתב לוגית: `ps/pe/ms/me`, `start/end` — לא `pl/pr/left/right`.

## Elevation & Depth

המערכת שטוחה כברירת מחדל. עומק נמסר דרך קווי שיער וטונליות (לבן על `#f6f9fc`), לא
דרך צללים. הצללים שקיימים הם מכוונים ומעטים, ולכולם יש היסט ופיזור רך — אין הילות
צבע בלי היסט ואין צללים חדים.

### Shadow Vocabulary
- **צל טופס** (`box-shadow: 0 20px 48px rgba(13,37,61,0.14)`): כרטיס הטופס. הצל
  הגדול היחיד באתר. בדסקטופ הוא מתהפך למעלה (`0 -20px 48px`) כי הכרטיס עולה מהקפל.
- **הילת כפתור מותג** (`0 12px 28px rgba(0,0,255,0.25)`): רק כפתורי `variant="brand"`.
- **תגית צפה** (`0 10px 24px rgba(0,0,120,0.25)`): ה-pill הכחול שיושב על שפת כרטיס
  הטופס.
- **הרמת כרטיס ב-hover** (`0 12px 32px rgba(13,37,61,0.12)`): כרטיסי משאבים/כתבות
  שהם קישורים אמיתיים.

### Named Rules

**חוק הצל היחיד.** באתר יש אובייקט אחד שצף — כרטיס הטופס. כל השאר יושב על הנייר. כל
צל שנוסף למשטח שאינו הטופס מוריד את הטופס בהיררכיה.

**חוק הצל-כתגובה.** משטחים שטוחים במנוחה. צל חדש מותר רק כתגובה למצב (hover על
קישור), לא כקישוט במנוחה.

## Shapes

שפת הצורה מבוססת על שני קצוות: **pill מלא** (`9999px`) לכל מה שאינטראקטיבי או מתייג
— כפתורים, תגיות, צ'יפים, קישורי טלפון — ו**פינות מעוגלות מדודות** לכל מה שמכיל
תוכן: `12px` לכרטיס רגיל, `16px` לכרטיס שקט, `18px` (`--radius-card`) לכרטיס גדול
וסקשני באנר, `20–28px` לכרטיס ההירו, `10px` לשדה קלט.

הגבול הוא תמיד `1px solid var(--color-hairline)`. גבול עבה יותר קיים בשני מקרים
מוצדקים בלבד: מסגרת `1.5px` בכפתור `outline-accent`, ו-`border-s-[5px]` בכחול על
תיבת "בשורה התחתונה" — הרמז הגרפי לסימון בשוליים של מסמך.

**חוק הקצה הכפול.** אינטראקטיבי = עגול לגמרי. מכיל תוכן = פינה מדודה. אין ביניים; צ׳יפ
עם `rounded-lg` הוא שגיאת מערכת.

## Components

### Buttons
- **Shape:** pill מלא (`9999px`) בכל הווריאנטים והגדלים, ללא יוצא דופן.
- **Brand (ראשי):** כחול טופס על לבן, `min-height: 50px`, `padding: 12px 28px`,
  משקל 700, עם הילת הכפתור הכחולה. זה כפתור ה"בדיקת זכאות".
- **Accent:** אדום סימון על לבן — לפעולה חמה יחידה בסקשן אדום. לא מתחרה בכחול באותו
  שדה ראייה.
- **Outline:** לבן עם קו שיער, משקל 400; ב-hover הגבול והטקסט עוברים לכחול. פעולה
  משנית.
- **Outline-accent:** מסגרת `1.5px` אדומה, טקסט `#c93330`, מילוי `#fdf2f2` ב-hover.
- **Ghost / Link:** ללא רקע; `link` בכחול עם קו תחתון ב-hover.
- **Sizes:** `sm` 44px (רצפת מגע), `default` 50px, `lg` 52px, `icon` 44×44.
- **Focus:** טבעת גלובלית `2px solid #0000ff` עם `outline-offset: 2px` דרך
  `:focus-visible` — לא מוגדרת פר-כפתור.

### Chips
- **Brand chip:** רקע `#eef0ff`, טקסט כחול, משקל 700 — קטגוריות מגזין, מספרי שלבים.
- **Accent chip:** רקע `#fdf2f2`, טקסט `#c93330`, 13px — תגיות שירות.
- **Outline chip:** לבן/`#f6f9fc` עם קו שיער — "זכויות משיקות".
- **State:** צ׳יפ שהוא קישור מקבל `hover:border-brand hover:text-brand`. **צ׳יפ שאינו
  קישור לא מקבל hover בכלל** — hover על מה שאינו לחיץ הוא הבטחה שנשברת.

### Cards / Containers
- **Corner:** `12px` (רגיל), `16px` (שקט), `18px` (גדול/באנר).
- **Background:** לבן על נייר קריר, או נייר קריר על לבן — הכרטיס תמיד בניגוד טונלי
  לסקשן שמכיל אותו.
- **Shadow:** ללא, כברירת מחדל (ראו חוק הצל היחיד). כרטיס שהוא קישור מקבל הרמה
  ב-hover.
- **Border:** `1px solid #e3e8ee`.
- **Padding:** `24px` (`p-6`) כברירת מחדל, `28px` לכרטיסי תוכן רחבים.

### Inputs / Fields
- **Style:** רקע לבן, גבול `1px #cbd5e1`, פינות `10px`, גובה מינימלי 48px, טקסט 16px
  (מונע זום אוטומטי ב-iOS).
- **Label:** תווית **קבועה** מעל השדה, תמיד. placeholder הוא דוגמה, לא תווית.
- **Focus:** טבעת המותג הגלובלית.
- **Error:** `aria-invalid` הופך את הגבול לאדום סימון, והודעה מילולית בעברית מתחת
  לשדה שמסבירה מה לתקן — לא רק שמשהו שגוי.
- **Numeric:** `inputmode` מתאים לטלפון, ו-`.tnum` על הערך.

### Navigation
- **Desktop:** שורה לבנה עם קו שיער תחתון; 5 קישורים ב-17px `#273951`; הפריט הפעיל
  מקבל קו תחתון אדום `2px` ומשקל 700 — אותו דקדוק של הקו האדום שמסמן. בקצה: טלפון
  עם עיגול אדום, ואחריו כפתור מותג.
- **Mobile:** המבורגר (44×44) · לוגו · עיגול פעולה כחול (44×44). התפריט נפתח כשכבה
  מלאה.
- **Mobile CTA bar:** רצועה דביקה תחתונה — חיוג · וואטסאפ · בדיקת זכאות — שנעלמת
  כשהטופס עצמו נראה, כדי לא להתחרות בו.

### כרטיס הטופס (Signature Component)
האובייקט המרכזי של המערכת. כרטיס לבן, פינות `20px` (בדסקטופ `28px` למעלה בלבד, בלי
גבול תחתון — הוא "עולה" מהקפל), צל הטופס הגדול, ותגית pill כחולה שיושבת **על** השפה
העליונה (`-top-21px`) ומכריזה על ההבטחה. `BorderBeam` עדין רץ על המסגרת בדף הבית
בלבד. כל שאר האתר בנוי כדי להחזיר אליו.

### רצועת המדדים הנייבי (Signature Component)
משטח `#122680` בפינות `18px`, עם עיגול גרדיאנט כחול-אל-שקוף בפינה כרמז אור. מספרים
ב-RaananPro 900 בגדלים 34–46px עם `.tnum`, מדד אחד מודגש באדום סימון, ותוויות
`rgba(255,255,255,0.9)`. זה הפורמט שמחזיק כל טענה מספרית באתר.

## Do's and Don'ts

### Do:
- **Do** להשתמש ב-`SectionHeading` עם `strong`/`underlineStrong` לכל כותרת סקשן —
  התבנית הטיפוגרפית של המותג היא קומפוננטה, לא הרגל.
- **Do** לתת `.tnum` לכל מספר-נתון.
- **Do** לכתוב כל מרווח כיווני לוגית (`ps/pe/ms/me`) — האתר RTL.
- **Do** לשמור על רצפת מגע 44×44 לכל דבר לחיץ, גם כשהמראה קטן יותר (הרחבה דרך
  `::before` או `min-h-11`).
- **Do** לרנדר תוכן **גלוי** ולהוסיף אנימציה רק כשה-JS חי (`:root[data-js]`) — קהל
  היעד מגיע במכשירים איטיים.
- **Do** לתת תווית קבועה לכל שדה טופס.
- **Do** להשתמש ב-`#616f88` כרצפה לטקסט עמום, לא כנקודת התחלה.

### Don't:
- **Don't** לרכך את `#0000ff` לשום כחול נוח יותר.
- **Don't** למלא שטח גדול באדום סימון — הוא מסמן, לא ממלא.
- **Don't** להוסיף צל למשטח שאינו כרטיס הטופס או קישור ב-hover.
- **Don't** להשתמש באפור נייטרלי (`#64748b`, `slate-*`, `gray-*`) בשום מקום.
- **Don't** לשים תגית/eyebrow מעל כותרת.
- **Don't** לתת `hover` לאלמנט שאינו לחיץ.
- **Don't** להשתמש בגליף יוניקוד (`›`, `‹`, `←`, `✓`, `💡`) כתחליף לאייקון — אייקונים
  מצוירים כ-SVG במשקל קו אחיד, ובכיוון שמתהפך נכון ב-RTL.
- **Don't** להשתמש בגרדיאנט על טקסט או בזכוכית/blur כקישוט.
- **Don't** להוסיף אנימציית כניסה זהה לכל סקשן — רגע אחד מכוון, והשאר שקט.
- **Don't** לתת לתוכן גוף לעבור 75 תווים לשורה.
