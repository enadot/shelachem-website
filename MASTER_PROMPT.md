# מאסטר־פרומפט — אתר ״שלכם — מימוש זכויות רפואיות״

מסמך אחד שמכיל את כל מה שצריך כדי לבנות את האתר הזה מאפס. הוא נכתב כדי להימסר
כהודעה ראשונה לסוכן פיתוח (Claude Code / Cursor / כל סוכן אחר), או להישמר כ-`CLAUDE.md`
בשורש ריפו חדש.

**איך משתמשים בו:** מוסרים את המסמך כולו, מוודאים שסעיף §17 (נכסים) סופק, עונים על
§18 (שאלות פתוחות), ואז מבקשים תוכנית עבודה לפי §15 לפני שכותבים קוד.

**חוק ברזל אחד:** כל החלטה שלא כתובה כאן — שואלים. כל החלטה שכתובה כאן — מקיימים
כלשונה. מערכת העיצוב (§4) אינה המלצה; היא מפרט.

---

## 1. הבריף — החברה

**״שלכם — מימוש זכויות רפואיות בע״מ״** — חברה ישראלית שנוסדה ב-2013 ומלווה אנשים
במיצוי זכויות רפואיות מול הגופים שמחזיקים בכסף שלהם: ביטוח לאומי, רשות המסים,
קרנות פנסיה, חברות ביטוח ומשרד הרישוי.

| נתון | ערך |
|---|---|
| שם משפטי | שלכם — מימוש זכויות רפואיות בע״מ |
| שם קצר | שלכם |
| דומיין | `https://www.shelachem.net` |
| טלפון ראשי | 076-8017778 |
| וואטסאפ | `https://wa.me/972768017778` |
| דוא״ל | info@shelachem.net |
| שנת ייסוד | 2013 |
| שנות פעילות מוצהרות | 13 |
| לקוחות מוצהרים | 10,059 |
| מודל תשלום | שכר טרחה רק בהצלחה · בדיקה ראשונית חינם · 0 ₪ מראש |
| סניפים | ירושלים — בניין שערי העיר, רח׳ יפו פינת שרי ישראל, קומה 9 · בני ברק — ז׳בוטינסקי 168 · טל׳ 03-5430302 |
| אזור שירות | כל ישראל (פיזית בשני סניפים, טלפונית ובזום בכל הארץ) |

**הבידול:** צוות רפואי אמיתי — מעל 100 רופאים ומומחים שכותבים חוות דעת, מכינים
לקוחות לוועדות רפואיות ומנתחים פרוטוקולים לצורך ערר. זה מה שמבדיל בין ״חברה
שממלאת טפסים״ לבין ״חברה שמנצחת ועדות״, וזה צריך להישמע בכל עמוד.

### קהל היעד

אדם במצוקה רפואית או בן משפחה שלו, בדרך כלל בגילי 45–70, שכבר מוקף בטפסים ובלשון
פקידותית. לעיתים קרובות במכשיר אנדרואיד ישן וברשת איטית. הוא לא מחפש חוויית מוצר —
הוא מחפש כתובת אחת ברורה, בן אדם שיחזור אליו, והבטחה שלא ייתקע לבד.

**פרסונת הבקרה — ״רבקה״:** בת 58, אנדרואיד ישן, 3G, קוראת לאט, חוששת שיגבו ממנה
כסף. **כל החלטת UX נבחנת מולה:** אם משהו לא עובד בשבילה — הוא לא נכנס.

### הטון

אמפתי-בוטח: ״מגיע לכם — ואנחנו נשיג את זה בשבילכם״. עברית מדוברת, פנייה בגוף שני
רבים. לא משפטי-יבש, לא מכירתי-אגרסיבי, ובלי הבטחות שלא נמסרות. שני משפטי-הבית של
המותג: **״מגיע לכם לדעת מה מגיע לכם״** ו**״שכר טרחה רק בהצלחה״**.

---

## 2. יעדי המוצר

האתר הוא **אתר לידים**. הצלחה נמדדת ב:

1. **המרה לליד** — טופס (שם, טלפון, דוא״ל) או שיחת טלפון. כל מסלול באתר חייב
   להיגמר באחד מהשניים.
2. **תנועה אורגנית long-tail** — עמודי שירות, עמודי מוסד ומגזין שמדורגים על
   שאלות אמיתיות (״האם מגיע לי פטור ממס בגלל סוכרת״).
3. **הופעה בתשובות AI (GEO)** — האתר צריך להיות מקור שמנועי מענה מצטטים.
4. **נגישות ואמון** — תקן ישראלי ת״י 5568 / WCAG 2.1 AA, ללא יוצא מן הכלל.

**ה-CTA היחיד באתר:** בדיקת זכאות. כל כפתור ראשי מוביל אליה. אין CTA מתחרה.

---

## 3. הסטאק והמבנה

### טכנולוגיה

- **Next.js (App Router) + TypeScript + Tailwind CSS v4** — RTL מלא, `dir="rtl"` על `<html>`.
- **shadcn/ui** לפרימיטיבים (Button, Input, Label, Checkbox, Select, Slider, Dialog,
  Accordion, Card) על בסיס Radix. **Magic UI** לשני אפקטים בלבד: `BorderBeam`
  (מסגרת כרטיס הטופס בדף הבית) ו-`NumberTicker` (מדדים).
- **טפסים:** react-hook-form + zod, הודעות שגיאה בעברית.
- **אנימציה:** CSS + IntersectionObserver בלבד (ראו §12). framer-motion מותר רק אם
  יש לו הצדקה ספציפית — לא כברירת מחדל.
- **CMS:** Directus self-hosted (Docker + Caddy) על VPS, עם נפילה חזרה לתוכן מקומי מוקלד.
- **רינדור:** SSG/ISR. `export const revalidate = 3600` ברמת ה-layout + webhook לרענון מיידי.
- **פריסה:** Vercel (או כל מארח Next.js), CMS על VPS נפרד תחת `cms.<domain>`.

> ⚠️ גרסאות Next.js משתנות מהר ושוברות API. **לפני כתיבת קוד — קוראים את המדריך
> הרלוונטי ב-`node_modules/next/dist/docs/`.** אל תסמכו על זיכרון.

### מבנה תיקיות

```
src/
  app/
    layout.tsx              # RTL, מטא גלובלי, JSON-LD, Header/Footer/MobileCtaBar, skip-link
    page.tsx                # דף הבית
    globals.css             # טוקנים (@theme), @font-face, כללי מותג
    about/ team/ doctors/ branches/ faq/ magazine/ institutions/ services/
    privacy/ accessibility/ not-found.tsx error.tsx
    api/leads/route.ts      # קליטת לידים
    api/revalidate/route.ts # webhook מה-CMS
    api/cms-status/route.ts # אבחון חיבור ל-CMS
    sitemap.ts robots.ts llms.txt/route.ts
  components/
    layout/    header, footer, mobile-menu, mobile-cta-bar, contact-modal(+context)
    home/      hero, specialties, banners, how-it-works, story, testimonials,
               magazine, community, final-cta, faq-section
    shared/    lead-form, lead-modal, lead-cta, section-heading, reveal, breadcrumb,
               faq-accordion, carousel, article-card, article-image, cms-image,
               testimonial-card, person-avatar, navy-hero, page-cta, icons
    ui/        shadcn primitives
    magicui/   border-beam, number-ticker
    doctors/ services/ institutions/ magazine/ branches/   # קומפוננטות ייעודיות
  lib/
    config.ts       # מקור אמת יחיד: טלפון, כתובות, סניפים, מדדים
    content/        # index.ts (שכבת גישה), types.ts, local/*.ts (תוכן fallback)
    directus.ts schema.ts analytics.ts faq-groups.ts utils.ts
public/fonts/ public/images/
directus/           # docker-compose, Caddyfile, seed scripts, schema.md
```

### כללי עבודה

- Mobile-first. שבירה עיקרית `md` = 768px (שם משתנים הניווט, ההירו והסיידברים).
- קומיטים קטנים ומתועדים בעברית או באנגלית — עקביים.
- אין התקנת ספריות מעבר למוסכם בלי לשאול.
- CI על כל push/PR: `tsc --noEmit` + `eslint` + `next build` (ה-build רץ **בלי**
  משתני סביבה, כדי לוודא שהאתר עובד על התוכן המקומי בלבד).

---

## 4. מערכת העיצוב (מחייבת)

**כוכב הצפון היצירתי: ״הפקיד שבצד שלכם״.** המערכת לוקחת את אוצר המילים החזותי של
הבירוקרטיה הישראלית — כחול טופס, מספרים מיושרים, שדות מוקפדים — ומסיטה אותו 180
מעלות: אותה סמכות, רק שהפעם היא עובדת בשביל המשתמש.

**אנטי-רפרנסים מוצהרים:** זו **לא** קטגוריית ״משרד עורכי דין״ (בלי זהב, בלי סריפים
קלאסיציסטיים, בלי דימויי סמכות) וגם **לא** SaaS (בלי גרדיאנטים סגולים, בלי זכוכית,
בלי איורים אקסונומטריים).

### 4.1 צבע

```css
/* Brand */
--color-brand:            #0000ff;  /* כחול טופס טהור — הקול הראשי */
--color-brand-hover:      #0000c9;
--color-brand-deep:       #122680;
--color-banner:           #122680;  /* משטחי נייבי */

/* Ink — סקאלת דיו כחלחלה. אין אפור נייטרלי באתר. */
--color-ink:              #0d253d;  /* כותרות */
--color-ink-secondary:    #273951;  /* גוף טקסט */
--color-ink-muted:        #4a5b74;  /* קישורי פוטר, עזר */
--color-ink-faint:        #616f88;  /* מטא־דאטה — רצפה, לא נקודת התחלה (4.8:1) */

/* Surfaces */
--color-surface:          #f6f9fc;  /* נייר קריר */
--color-surface-blue:     #eef0ff;  /* תגיות כחולות, מספרי שלבים */
--color-hairline:         #e3e8ee;
--color-hairline-soft:    #eef1f6;

/* Accent — אדום סימון */
--color-accent:           #d93a38;  /* AA מול לבן: 4.55:1 */
--color-accent-hover:     #c22f2d;
--color-accent-tint:      #fdf2f2;
--color-accent-tint-strong:#fde3e2;
--color-accent-text:      #c93330;  /* טקסט אדום על לבן/נייר: 5.25:1 / 4.97:1 */
--color-accent-ink:       #b32926;  /* טקסט אדום על גוון אדום: 5.29:1 */
--color-accent-on-navy:   #e75f5d;  /* מספרים גדולים על נייבי: 3.84:1 */
```

**חוקים בעלי שם:**

- **חוק הכחול הטהור.** `#0000ff` לא מתרכך ל-`#2563eb` ולא לשום ״כחול מוצר״ נוח.
  הטהרה היא הזהות.
- **חוק הקו האדום.** האדום **מסמן ולא ממלא** — קו תחתון עבה (4–6px) מתחת למילת
  מפתח, תגית קטנה, עיגול טלפון. לעולם לא רקע של סקשן שלם. יותר מ-10% מהמסך = כשל.
  (החריג היחיד: הבאנר האדום בדף הבית, שהוא רגע יחיד ומכוון.)
- **חוק אפס-אפור.** אין `#64748b`, אין `slate-*`, אין `gray-*`. כל טקסט משני, גבול
  ורקע נגזרים מסקאלת הדיו.
- **וריאנט לפי רקע.** לכל צבע אדום יש וריאנט מותאם לרקע שעליו הוא יושב. לא בוחרים
  ״האדום״ — בוחרים את האדום של הרקע הזה.

### 4.2 טיפוגרפיה

שני פונטים עבריים, מוגשים מקומית כ-woff2 עם `font-display: swap`:

- **RaananPro** (display, משקלים 300/400/700/900) — כותרות בלבד.
- **SimplerPro** (body, משקלים 300/400/600/700/900) — כל השאר.

| רמה | פונט | גודל | משקל | line-height |
|---|---|---|---|---|
| Display | RaananPro | `clamp(40px, 4.45vw, 64px)` | 300 | 1.08 |
| Headline | RaananPro | `clamp(26px, 3vw, 36px)` | 300 | 1.15 |
| Title | SimplerPro | 21px | 700 | 1.3 |
| Body | SimplerPro | 16.5–17px | 400 | 1.625 |
| Body-Read | SimplerPro | 18px | 400 | 1.75 |
| Label | SimplerPro | 15px | 700 | 1.2 |

**חוקים בעלי שם:**

- **חוק 300/900.** כותרת לעולם לא מודגשת כולה ולא קלה כולה: היא נכתבת ב-300, ומילת
  המפתח שלה — ורק היא — נושאת 900 וקו תחתון אדום. זו החתימה של המותג.
  ```css
  .keyword-underline { font-weight: 900; border-bottom: 6px solid var(--color-accent); line-height: 1; }
  @media (max-width: 767px) { .keyword-underline { border-bottom-width: 4px; } }
  ```
  מיושם דרך קומפוננטת `SectionHeading` עם `strong` / `underlineStrong`. **אין
  לשחזר את התבנית ידנית.**
- **חוק ה-tnum.** כל מספר שהוא נתון (סכומים, אחוזי נכות, טלפונים, מספרי שלבים,
  מדדים) מקבל `.tnum` → `font-feature-settings: "tnum"`. מספרים שרוקדים ברוחב הם
  ההבדל בין דוח לבין באנר.
- **חוק המדידה.** גוף טקסט לא עובר ~75 תווים לשורה. מגזין: מכל 1040px. עמודי
  שירות: פסקאות עד 68ch.
- **איסור eyebrow.** אין תגית/קיקר מעל כותרת. הכותרת נושאת את עצמה.

### 4.3 פריסה

- מכל `1240px` לעמודי מוצר, `1040px` לעמודי קריאה — שניהם ממורכזים.
- שוליים: `24px` במובייל, `clamp(24px, 6.7vw, 96px)` בדסקטופ.
- מקצב אנכי: `py-48px` מובייל / `py-64px`–`88px` דסקטופ לכל סקשן.
- **הפרדת סקשנים בשתי דרכים בלבד:** החלפת רקע לבן↔`#f6f9fc`, או רצועת נייבי.
  **אין קווים מפרידים בין סקשנים.**
- גרידים דומיננטיים: `[1fr 380px]` (עמוד שירות), `[1fr 260px]` (כתבה),
  `[240px 1fr]` (FAQ) — עמוד ראשי + סיידבר דביק (`top: 96px`) שנקרס מתחת ל-768px.
- **RTL:** כל מרווח כיווני נכתב לוגית — `ps/pe/ms/me`, `start/end`. **לא**
  `pl/pr/left/right`.

### 4.4 עומק, צורה, קומפוננטות

**חוק הצל היחיד.** באתר יש אובייקט אחד שצף — **כרטיס הטופס**. כל השאר יושב על
הנייר. עומק נמסר דרך קווי שיער וטונליות, לא דרך צללים.

| צל | ערך | שימוש |
|---|---|---|
| צל טופס | `0 20px 48px rgba(13,37,61,.14)` | כרטיס הטופס (בדסקטופ בהירו מתהפך ל-`0 -20px 48px`) |
| הילת כפתור מותג | `0 12px 28px rgba(0,0,255,.25)` | כפתור `brand` בלבד |
| תגית צפה | `0 10px 24px rgba(0,0,120,.25)` | ה-pill הכחול על שפת כרטיס הטופס |
| הרמת כרטיס ב-hover | `0 12px 32px rgba(13,37,61,.12)` | כרטיס שהוא קישור אמיתי |

**חוק הקצה הכפול.** אינטראקטיבי = pill מלא (`9999px`). מכיל תוכן = פינה מדודה:
`10px` שדה קלט · `12px` כרטיס רגיל · `16px` כרטיס שקט · `18px` (`--radius-card`)
כרטיס גדול/באנר · `20–28px` כרטיס ההירו. אין ביניים.

**כפתורים** — pill בכל הווריאנטים. `brand` (כחול, 50px, משקל 700, עם הילה) ·
`accent` (אדום — פעולה חמה יחידה בשדה ראייה) · `outline` (לבן + קו שיער, hover
לכחול) · `outline-accent` (מסגרת 1.5px אדומה) · `ghost` / `link`.
גדלים: `sm` 44px (רצפת מגע), `default` 50px, `lg` 52px, `icon` 44×44.

**פוקוס** — טבעת גלובלית אחת, לא פר-כפתור:
```css
:root { --focus-ring: var(--color-brand); }
.surface-navy { --focus-ring: #ffffff; }   /* כחול על נייבי = 1.51:1 — נכשל ב-SC 1.4.11 */
:focus-visible { outline: 2px solid var(--focus-ring); outline-offset: 2px; }
```

**שדות קלט** — רקע לבן, גבול `1px #cbd5e1`, פינות `10px`, גובה ≥48px, טקסט 16px
(מונע זום ב-iOS). **תווית קבועה מעל השדה, תמיד** — placeholder הוא דוגמה, לא תווית.
שגיאה: `aria-invalid` + הודעה מילולית בעברית עם `role="alert"` שמסבירה מה לתקן.

**צ׳יפים** — brand (`#eef0ff` + טקסט כחול), accent (`#fdf2f2` + `#c93330`, 13px),
outline. **צ׳יפ שאינו קישור לא מקבל hover בכלל** — hover על מה שאינו לחיץ הוא
הבטחה שנשברת.

### 4.5 שתי קומפוננטות החתימה

**כרטיס הטופס.** האובייקט המרכזי של המערכת. כרטיס לבן, פינות `20px` (בדסקטופ
`28px` למעלה בלבד, בלי גבול תחתון — הוא ״עולה״ מהקפל), צל הטופס הגדול, ותגית pill
כחולה שיושבת **על** השפה העליונה (`-top-21px`) ומכריזה על ההבטחה. `BorderBeam` עדין
רץ על המסגרת **בדף הבית בלבד**. כל שאר האתר בנוי כדי להחזיר אליו.

**רצועת המדדים הנייבי.** משטח `#122680` בפינות `18px`, עם עיגול גרדיאנט
כחול-אל-שקוף בפינה כרמז אור. מספרים ב-RaananPro 900 בגדלים 34–46px עם `.tnum`,
מדד אחד מודגש ב-`--color-accent-on-navy`, תוויות `rgba(255,255,255,.9)`. זה הפורמט
שמחזיק כל טענה מספרית באתר.

### 4.6 Do / Don't

**Do:** להשתמש ב-`SectionHeading` לכל כותרת סקשן · `.tnum` לכל מספר-נתון · מרווחים
לוגיים · רצפת מגע 44×44 לכל דבר לחיץ (גם כשהמראה קטן יותר) · לרנדר תוכן **גלוי**
ולהוסיף אנימציה רק כשה-JS חי · תווית קבועה לכל שדה.

**Don't:** לרכך את `#0000ff` · למלא שטח גדול באדום · להוסיף צל למשטח שאינו כרטיס
הטופס · אפור נייטרלי · eyebrow מעל כותרת · hover על מה שאינו לחיץ · גליף יוניקוד
(`›`, `←`, `✓`, `💡`) כתחליף לאייקון — אייקונים מצוירים כ-SVG במשקל קו אחיד ומתהפכים
נכון ב-RTL · גרדיאנט על טקסט · זכוכית/blur כקישוט · אנימציית כניסה זהה לכל סקשן.

---

## 5. ארכיטקטורת מידע — מפת האתר

```
/                          דף הבית
/about                     הסיפור שלנו
/team                      צוות ההנהלה
/doctors                   הרופאים והמומחים
/institutions              Hub — מוסדות ובירוקרטיה
/institutions/[slug]       עמוד מוסד   (bituach-leumi, mas-hachnasa,
                                        karnot-pensia, hevrot-bituach, misrad-harishui)
/services/[slug]           עמוד שירות  (tax-exemption, …)
/magazine                  אינדקס המגזין
/magazine/[slug]           כתבה
/faq                       שאלות ותשובות (הכתובת הקנונית ל-FAQPage)
/branches                  סניפים ויצירת קשר
/privacy /accessibility    noindex
/api/leads /api/revalidate /api/cms-status
/sitemap.xml /robots.txt /llms.txt
```

**היררכיה:** `/services/[slug]` הוא עמוד-בן של `/institutions` — כך בפירורי הלחם
וכך בהדגשת פריט הניווט הפעיל.

**ניווט ראשי (5 פריטים):** בית · הסיפור שלנו · תחומי פעילות (`/institutions`) ·
שאלות ותשובות · מגזין.

---

## 6. Layout גלובלי

### 6.1 Header (sticky)

- `sticky top-0 z-30`, רקע `bg-white/95 backdrop-blur`, קו שיער תחתון. קומפקטי
  (לוגו 44px, `py-3`) — נוכחות קבועה בלי לגזול גובה, **בלי מאזין scroll ב-JS**.
- **דסקטופ:** לוגו · 5 קישורים ב-17px · הפריט הפעיל מקבל קו תחתון אדום `2px` +
  משקל 700 + `aria-current="page"` · בקצה: טלפון עם עיגול אדום, ואחריו כפתור מותג
  ״דברו איתנו״ שפותח מודאל ערוצים (וואטסאפ / טלפון / מייל).
- **מובייל:** המבורגר (44×44) · לוגו · עיגול כחול (44×44) ש**מחייג ישירות** —
  לא פותח מודאל. מי שרוצה לדבר עם בן אדם לא צריך צעד ביניים.
- תפריט מובייל: שכבה מלאה בגרדיאנט המותג, אייקון + שם לכל פריט, CTA אדום, טלפון.

### 6.2 Mobile CTA bar (sticky bottom)

שלושה כפתורים: **חייגו · וואטסאפ · בדיקת זכאות**. נעלם (`translate-y-full`) כאשר
`#lead-form` נראה על המסך (IntersectionObserver, threshold 0.2) — אין טעם להציע את
מה שכבר מול העיניים. יש spacer בגובה הבר כדי שלא יסתיר את תחתית הפוטר.
**לכן:** בכל עמוד שיש בו טופס — ל-wrapper שלו יש `id="lead-form"`.

### 6.3 Footer (מגה-פוטר, זהה בכל האתר)

לוגו + 4 עמודות: **מוסדות ובירוקרטיה** (5 מוסדות) · **אודות** (הסיפור/צוות/רופאים/סניפים)
· **ידע ותשובות** (כל הכתבות, מדריכים, סיפורי הצלחה, שאלות ותשובות) · **מחלות
וזכויות** (כל קישור מוביל לעמוד שבאמת מטפל בזכות — לא ל-`/institutions` הגנרי).
מתחת: שורת סניפים + טלפון + זכויות יוצרים + מדיניות פרטיות + הצהרת נגישות.
כל קישור פוטר `min-h-11` (יעד מגע WCAG 2.2).

### 6.4 layout.tsx

`<html lang="he" dir="rtl">` · skip-link ״דילוג לתוכן הראשי״ · סקריפט אינליין
שמסמן `data-js` על `<html>` (עם failsafe של 4ש׳) · JSON-LD גלובלי · `metadataBase`,
title template `%s | שלכם`, OG בעברית (`he_IL`) · `themeColor: #0000ff` ·
`export const revalidate = 3600`.

---

## 7. מפרט העמודים

### 7.1 דף הבית — `/`

סדר הסקשנים, מלמעלה למטה:

1. **Hero — חצי לבן / חצי כחול.**
   - **דסקטופ:** ימין (לבן) — `h1` ״13 שנות מקצוענות במימוש הזכויות **שלכם**״
     (״שלכם״ ב-`.keyword-underline`), ומתחתיו, צמוד לתחתית ההירו — כרטיס הטופס
     (§4.5). שמאל (כחול) — גרדיאנט המותג, תמונת צוות PNG שקוף מעוגנת לתחתית, פס
     ״רצפה״ כהה (`#000085→#0000ad`) מאחורי האנשים, ועיגול glow פועם (7s).
   - **מובייל:** `h1` על לבן ← בלוק כחול 340px עם התמונה ← כרטיס הטופס חופף את
     תחתית הכחול (`-mt-12`).
   - **ה-`h1` הוא אלמנט ה-LCP — אסור שיהיה עטוף באנימציית כניסה או תלוי בהידרציה.**
   - התמונה מנוהלת ב-CMS (`globals ▸ hero_image`) עם נפילה ל-`/images/hero-team.png`;
     החצי הכחול מוצג גם כשאין תמונה.
2. **תחומי התמחות** — רקע `#f6f9fc`. צ׳יפים לבנים עם אייקון קו לכל תחום: פטור ממס
   הכנסה, קצבת נכות כללית, אובדן כושר עבודה, פנסיית נכות, ביטוח סיעודי, מחלת מקצוע,
   תאונת עבודה, שמירת הריון, ילד נכה, שירותים מיוחדים, נפגעי פעולות איבה, זכויות
   אלמנים ויתומים, תאונות אישיות, תג חניה לנכה. צ׳יפ שהוא קישור → hover כחול.
3. **באנר אדום** — ״כנראה שמגיע לכם הרבה יותר ממה שאתם חושבים.״ + ״משאירים שם
   וטלפון, ואנחנו בודקים בשבילכם בדיוק איפה אתם עומדים.״ + קישור ״להשאיר פרטים
   לבדיקה →״.
4. **פסקת מהות** — ממורכז, ≤880px: ״מימוש זכויות רפואיות: **הופכים את הזכות שלכם
   למציאות.**״
5. **שלושה צעדים** (`#how-it-works`) — ״שלושה צעדים שלכם. **החלק הקשה שלנו.**״
   כרטיסים ממוספרים: משאירים פרטים / בודקים לעומק / אנחנו נכנסים בשבילכם.
   מתחת — כפתור מותג ״לבדיקה ראשונית חינם״.
6. **הסיפור שלנו** — טקסט + ויז׳ואל. ״מ-2013 אנחנו עושים דבר אחד **ועושים אותו עד
   הסוף.**״ קישור ״קצת יותר עלינו →״.
7. **באנר מוסדות (נייבי)** — 5 אייקונים עגולים, כל אחד **קישור** לעמוד המוסד.
8. **עדויות** — ״הם כבר לא מוותרים **על מה ששלהם.**״ 3 כרטיסים; במובייל
   scroll-snap עם `role="region"` + `tabIndex={0}` (גלילה במקלדת). CTA
   `outline-accent`: ״רוצים שנבדוק גם לכם?״.
9. **מגזין** — ״ידע זה כוח. **וכוח זה כסף שמגיע לכם.**״ קרוסלה (עד 8 כתבות) עם
   חצי ניווט בדסקטופ ו-swipe במובייל + קישור ״לכל הכתבות״.
10. **רשתות חברתיות** — רקע `#f6f9fc`, ״אל תפספסו שום עדכון רלוונטי״ + כפתורי pill.
11. **CTA סופי** — ״בואו לבדוק מה מגיע לכם״ + טופס אינליין, עם לבבות watermark
    כחולים בשקיפות 5–7%.
12. **שאלות ותשובות** — **רק השאלות הפותחות** + כפתור ״לכל השאלות והתשובות — עוד N״.
    ה-`FAQPage` JSON-LD חי ב-`/faq` בלבד (כתובת קנונית אחת מדורגת טוב יותר משתיים).

### 7.2 `/about` — הסיפור שלנו

הירו · סיפור + ציר זמן · ערכים · רצועת מדדים נייבי · תהליך העבודה · אחריות חברתית · CTA.

### 7.3 `/team` — צוות ההנהלה

`NavyHero` (״הנהגת **שלכם**״) · גריד כרטיסי אנשים (אווטאר + שם + תפקיד + ביו +
קישור לינקדאין עם `aria-label` מלא) · תיבת ״רוח שלכם״ באדום-בהיר · `PageCta`.

### 7.4 `/doctors` — הרופאים והמומחים

`NavyHero` (״הרופאים שעומדים **מאחוריכם**״) · ״למה רופא מומחה **מכריע את התיק**״
(3 שלבים: חוות דעת / הכנה לוועדה / מעקב עד להחלטה) · גריד רופאים עם **צ׳יפי סינון
לפי התמחות** (`aria-pressed`) · הערת ״הצוות המלא מונה מעל 100 רופאים״ · `PageCta`.
התמחויות: internal, ortho, cardio, neuro, psych, onco.
JSON-LD: גרף של `Physician`.

### 7.5 `/institutions` — Hub

`NavyHero` (״כל מוסד, והדרך **לנצח בו**״) · גריד כרטיסי מוסדות (שם, tagline, תיאור,
צ׳יפים של שירותים, ״לעמוד המוסד →״) · רצועת הרגעה (13 / 10,059 / 0 ₪) · טופס על
משטח נייבי עם בחירת מוסד (`variant="dark"`, `.surface-navy` לטבעת פוקוס לבנה).

### 7.6 `/institutions/[slug]` — תבנית מוסד

`NavyHero` עם פירורי לחם · **איך אנחנו עובדים** (`<ol>` בן 4 שלבים ממוספרים) ·
**במה אנחנו מטפלים מול X** (כרטיסי שירות; כרטיס שמוביל לעמוד שירות = קישור, כרטיס
בלי עמוד = פותח את הטופס עם `presetTopic`) · **רצועת מדדים נייבי** · **טופס** עם
`topicOptions` מ-`formOptions` של המוסד + קישור ל-FAQ.

### 7.7 `/services/[slug]` — תבנית שירות (עמוד הכסף)

פריסה `[1fr 380px]`:

- **הירו לבן** — פירורי לחם (בית / מוסד / שירות), `h1` עם מילת מפתח מודגשת, אינטרו.
- **״מה חשוב לדעת — בשורה התחתונה״** — תיבה עם `border-s-[5px]` כחול (הרמז הגרפי
  לסימון בשוליים של מסמך), 2–4 עובדות עם אייקון ✓.
- **״מי זכאי?״** — רשימה ממוספרת בעיגולי `--color-accent-tint-strong` + טיפ.
- **״מצבים שכדאי להכיר״** — פסקאות עם כותרות משנה.
- **מחשבון** (אם `hasTaxCalculator`) — משטח נייבי, slider הכנסה + בחירת אחוזי נכות
  + שנים אחורה → חיסכון שנתי והחזר רטרואקטיבי. **המחשבון הוא הערכה ומוצג ככזה.**
- **עדויות** (קרוסלה עם חצים גם בדסקטופ) · **שאלות נפוצות** · **״להעמיק בנושא״**
  (כרטיסי משאבים שנגזרים מהכתבות שבאמת פורסמו — כותרת נלקחת מהכתבה עצמה; כתבה בלי
  גוף לא מוצגת).
- **סיידבר דביק:** כרטיס הטופס (`id="lead-form"`, תגית ״בודקים זכאות — חינם״) ·
  ״למה שלכם?״ (13 / 10,059 / 0 ₪) · ״זכויות משיקות״ (צ׳יפים) · ״מעדיפים לדבר?״ + טלפון.
- **`PageCta`** בתחתית.
- JSON-LD: `Service` + `FAQPage`.

### 7.8 `/magazine` — אינדקס

כותרת + פירורי לחם · **כתבה מרכזית** (כרטיס כהה רחב עם תגית ״הכתבה המרכזית״) ·
גריד כתבות עם **סינון קטגוריה בצד הלקוח** (`?cat=`, עטוף ב-`Suspense` כדי לשמור על
עמוד סטטי) · סיידבר דביק: ״הנקראים ביותר״ + כרטיס CTA נייבי.
קטגוריות: מדריכים · חדשות · סיפורי הצלחה.

### 7.9 `/magazine/[slug]` — כתבה

מכל 1040px, פריסה `[1fr 260px]`:
פירורי לחם · תגית קטגוריה + ״עודכן ב… · N דק׳ קריאה״ · `h1` (LCP — ללא אנימציה) ·
תקציר · מחבר · תמונת הירו · **״בשורה התחתונה״** (2–4 נקודות — הפורמט שמנועי מענה
מצטטים) · **תוכן עניינים מתקפל במובייל** ודביק בדסקטופ · גוף הכתבה (18px/1.75,
כל סקשן עם `id` לעוגן) · **CTA באמצע הכתבה** על משטח נייבי · ביו מחבר · **״המשיכו
לקרוא״** (3 כתבות). JSON-LD: `Article` עם `wordCount`, `datePublished`,
`dateModified`, `author`, `publisher`.

### 7.10 `/faq`

`NavyHero` · פריסה `[240px 1fr]` עם ניווט נושאים דביק · חמש קבוצות + ״שאלות
נוספות״ + קבוצה לכל שירות (שאלות ספציפיות מעמודי השירות, עם קישור ״לעמוד המלא של…״).
**בכוונה בלי אנימציית reveal** — זה עמוד עיון שמגיעים אליו מעוגן, והתוכן צריך פשוט
להיות שם. JSON-LD: `FAQPage` אחד עם כל השאלות.

קבוצות: `start` (לפני שמתחילים) · `process` (התהליך והזמנים) · `cost` (עלות ושכר
טרחה) · `rights` (זכויות וקצבאות) · `tax` (מס ואחוזי נכות). שאלה בלי `group` נופלת
ל״שאלות נוספות״ — כך תוכן שנוסף ב-CMS לא נעלם.

### 7.11 `/branches`

`NavyHero` (״קרובים אליכם, **בכל הארץ**״) · Explorer עם מפת ישראל SVG וסמנים לפי
סניף — כתובת, טלפון, הוראות הגעה, נגישות, Waze + Google Maps · תיבת ״רחוקים מסניף?
**ניפגש בזום.**״ + וואטסאפ · `PageCta`.

### 7.12 עמודים משפטיים ושגיאה

`/privacy` ו-`/accessibility` — `robots: { index: false }`, לא ב-sitemap.
`not-found.tsx` ו-`error.tsx` — בעברית, עם דרך חזרה וטלפון.

---

## 8. מודל התוכן ושכבת הגישה

### 8.1 העיקרון

```
DIRECTUS_URL מוגדר?  →  קורא מ-Directus  →  כשל/ריק  ┐
DIRECTUS_URL לא מוגדר ─────────────────────────────→ ┴→ תוכן מקומי מוקלד
```

**האתר חייב להיבנות ולעבוד מצוין בלי שום משתנה סביבה.** התוכן המקומי
(`src/lib/content/local/*.ts`) הוא טיפוסי, מלא, ומשקף את התוכן המאושר. כל כשל
ב-Directus נופל אליו בשקט — האתר לעולם לא נשבר בגלל ה-CMS.

### 8.2 טיפוסים (`src/lib/content/types.ts`)

```ts
Globals       { hero_image?, hero_image_alt? }
FaqItem       { id, question, answer, group? }
              group: "start"|"process"|"cost"|"rights"|"tax"
Testimonial   { id, quote, name, detail, image? }
Article       { id, slug, title, excerpt, category, readingMinutes, publishedLabel,
                publishedAt?, updatedAt?, image?, imageAlt?, keyPoints?[], featured?,
                author?{name,role,bio?}, body?[{id,heading?,paragraphs[],bullets?[]}],
                related?[slug] }
Doctor        { id, name, specialty, bio, image? }
TeamMember    { id, name, title, bio, linkedin?, image? }
Institution   { id, slug, name, tagline, description, services[{name,href?}],
                heroTitle, heroIntro, approach[{title,description}],
                serviceCards[{name,tag,description,href}], stats[{value,label,accent?}],
                statsNote?, formOptions[] }
Service       { id, slug, name, institutionSlug, heroIntro, takeaways[],
                eligibility[{title,description}], eligibilityTip?,
                sections[{id,heading,paragraphs[]}], hasTaxCalculator?,
                testimonials[], faqs[], relatedRights[{name,href?}],
                resources[{title,href,image?}] }
Lead          { full_name, phone, email?, topic?, source_page?, marketing_consent? }
```

### 8.3 כלל הפרסום

```ts
const isPublished = (a: Article) =>
  Boolean(a.body?.some(s => s.paragraphs?.length || s.bullets?.length));
```

**כתבה בלי גוף אינה מוצגת, אינה ב-sitemap ואינה בניווט.** עמוד שמכריז ״המדריך
המלא, שלב אחר שלב״ ומספק שתי שורות גרוע מ-404 — במיוחד בקטגוריה שכולה אמון, וגם
פוגע בדירוג של שאר האתר. ברגע שנוסף `body` — הכתבה חוזרת בלי שינוי קוד.

### 8.4 Directus

Collections זהים לטיפוסים: `faqs`, `testimonials`, `articles`, `doctors`,
`team_members`, `institutions`, `services`, `globals` (singleton), `leads`.

**הרשאות:** ל-role הציבורי — קריאה לכל collections התוכן (כולל `globals`
ו-`directus_files`), ו-**create בלבד** ל-`leads`.

**תמונות:** שדה העלאה `image_file` (מועדף) לצד שדה נתיב ישן `image`, ו-`image_alt`
לתיאור. ההגשה: `/assets/<id>?width=…&quality=75&format=auto` — sharp מקטין ומגיש
AVIF/WebP לפי הדפדפן, `next/image` בונה `srcset` דרך `CmsImage`, ו-`next.config.ts`
מוסיף `images.remotePatterns` ל-`/assets/**` לפי `DIRECTUS_URL`. Caddy מוסיף
`Cache-Control` לשבוע.
**כל נכס ממופה מחדש ל-host שב-`DIRECTUS_URL`** — גם אם נשמר ב-DB ככתובת מוחלטת —
כדי שהחלפת דומיין או מעבר ל-HTTPS לא תשבור תמונות.

**Webhook לרענון:** Flow ב-Directus על כל create/update/delete →
`POST /api/revalidate?secret=<REVALIDATE_SECRET>`.

**אבחון:** `/api/cms-status` מחזיר מאיזה host קוראים, אם `globals` נקרא (הרשאות),
אם תמונת ההירו נמצאה, וכמה פריטים חזרו מכל collection.

**HTTPS:** ה-CMS חייב לרוץ תחת `https://cms.<domain>` לפני עלייה לאוויר — אתר
HTTPS שמושך תמונות מ-`http://<ip>:8055` נחסם כ-mixed content.

---

## 9. הליד — הזרימה המלאה

### 9.1 הטופס (`LeadForm`)

שדות: **שם מלא\*** · **טלפון\*** · **דוא״ל** · (**נושא** — select אופציונלי, או
`presetTopic` שנקבע מראש) · צ׳קבוקס הסכמת שמירת פרטים (חובה) · צ׳קבוקס דיוור
(אופציונלי) · שדה honeypot מוסתר.

- **ולידציה (zod):** שם 2–100 · טלפון מנורמל (`+972`/רווחים/מקפים → ספרות עם 0
  מוביל) ובדיקת `^0\d{8,9}$` · דוא״ל תקין או ריק.
- **הודעות שגיאה בעברית שמסבירות מה לתקן** — ״נא להזין טלפון ישראלי תקין, לדוגמה:
  054-1234567״. כל הודעה עם `role="alert"` ו-`aria-describedby`.
- שדה טלפון: `type="tel"`, `inputMode="tel"`, `dir="ltr"`, `.tnum`, `autoComplete`.
- **מזהי שדות ייחודיים לפי `sourcePage`** — יש כמה טפסים בעמוד אחד.
- Layouts: `hero` (שם+טלפון בשורה, דוא״ל+שליחה בשורה) ו-`stacked` (סיידבר).
  Variants: `light` / `dark` (על נייבי).
- **מצב הצלחה:** הטופס מוחלף בהודעה עם `role="status"`: ״הפרטים התקבלו! נחזור
  אליכם ל-<טלפון> — בדרך כלל תוך יום עסקים אחד.״
- מיקרו-קופי קבוע: ״ללא עלות וללא התחייבות — שכר טרחה רק בהצלחה״.

### 9.2 ה-API (`POST /api/leads`)

ולידציה מחדש בשרת (zod) · honeypot חייב להיות ריק · כתיבה ל-Directus `leads` עם
`submitted_at` · **כשל בכתיבה לא מחזיר שגיאה למשתמש אלא נרשם ללוג עם כל הליד** —
לעולם לא לאבד ליד בשקט. ללא CMS: הליד נרשם ללוג ומוחזר `ok`.

### 9.3 שלושה מסלולי המרה

1. **טופס אינליין** — הירו, CTA סופי, סיידבר עמוד שירות, טופס עמוד מוסד.
2. **מודאל** (`LeadModalProvider` + `LeadCta`) — כל כפתור ״בדיקת זכאות״ שאינו
   ליד טופס פותח מודאל מעוצב עם אותו `LeadForm`. **אותה מילה = אותה התנהגות תמיד.**
3. **טלפון/וואטסאפ** — הדר, בר מובייל, מודאל ״דברו איתנו״, סיידבר.

### 9.4 אטריבוציה ומדידה

- `sourcePage` ייחודי לכל טופס (`home-hero`, `service-tax-exemption`,
  `article-cta`, `mobile-cta-bar`…), ופרמטרי `utm_*` מה-URL מצורפים אליו
  (`source|utm_...`, עד 200 תווים).
- `pushDataLayer({ event: "generate_lead", lead_source, lead_topic })` **רק אחרי
  שהשרת אישר** — אחרת סופרים המרות שלא קרו. עובד בלי תלות בספרייה: ברגע שיותקן
  GTM/GA4, יוצרים טריגר Custom Event ומכוונים להמרה, בלי שינוי קוד.

---

## 10. SEO ו-GEO

### 10.1 JSON-LD — גרף אחד

**מודול אחד** (`lib/schema.ts`) עם `@id` יציבים, מוזרק פעם אחת ב-layout:

```
ProfessionalService  #organization   ← הישות הראשית
WebSite              #website        ← publisher → הארגון
LocalBusiness        /branches#<id>  ← ישות לכל סניף, parentOrganization → הארגון
```

עמודים מצביעים ל-`@id` הקיים ב-**reference בלבד** (`{"@id": ORG_ID}`) ולא משכפלים
את הארגון. עמודי משנה מוסיפים: `Service` (עמוד שירות), `FAQPage` (`/faq` + עמוד
שירות), `Article` (כתבה), `Physician[]` (רופאים).

**כלל היושרה:** `sameAs` כולל רק פרופילים אמיתיים — קישור לדף הבית של רשת חברתית
מסונן. `openingHoursSpecification` ו-`geo` **נכללים רק כשיש נתונים אמיתיים** —
שעות או קואורדינטות שגויות ב-schema גרועות מהיעדרן, כי גוגל מציג אותן ומנועי AI
מצטטים אותן.

### 10.2 מטא

`metadataBase` · title template `%s | שלכם` · `alternates.canonical` **בכל עמוד** ·
description ייחודי · OG `he_IL` + תמונת ברירת מחדל 1200×630 · OG `article` עם
`publishedTime`/`modifiedTime` בכתבות.

### 10.3 sitemap / robots / llms.txt

- **sitemap** — נבנה מהתוכן האמיתי, `changeFrequency` נגזר מהתנהגות אמיתית של כל
  סוג עמוד (מגזין weekly, שירות/מוסד monthly, עמודי משנה yearly), `lastModified`
  מ-`publishedAt`. `/privacy` ו-`/accessibility` **לא** נכללים.
- **robots** — `allow: "/"`, `disallow: ["/api/"]`. **חסימה של `/privacy` ב-robots
  אסורה** — היא תמנע מגוגל לראות את ה-`noindex` עצמו. כלל מפורש נוסף מתיר במפורש
  את סוכני ה-AI (GPTBot, OAI-SearchBot, ChatGPT-User, ClaudeBot, Claude-SearchBot,
  Claude-User, Google-Extended, Applebot(-Extended), PerplexityBot, Bingbot,
  DuckAssistBot, MistralAI-User, meta-externalagent, Amazonbot, YouBot, cohere-ai).
  `Google-Extended` ו-`Applebot-Extended` מבינים **רק** חסימה — היתר מפורש הוא
  תיעוד וגם הגנה מפני חסימה גורפת עתידית.
- **`/llms.txt`** — אינדקס קריא-למודל (llmstxt.org), **מיוצר מהתוכן האמיתי** כדי
  שלא יתיישן: עובדות על החברה, מוסדות, שירותים, FAQ, כתבות, עמודי אודות.

### 10.4 GEO — כתיבה לציטוט

- **״בשורה התחתונה״** בראש כל כתבה (2–4 עובדות) ו-״מה חשוב לדעת״ בעמוד שירות —
  זה הפורמט שמנועי מענה מצטטים הכי הרבה, וגם מה שקורא ממהר צריך.
- שאלות ותשובות בכתובת קנונית אחת (`/faq`), לא מפוזרות.
- מספרים תמיד עם הקשר ומקור (״נתון שהחברה מצהירה עליו״).

---

## 11. נגישות — ת״י 5568 / WCAG 2.1 AA

לא בדיקה בסוף; אילוץ בזמן בנייה.

- **ניגודיות** — כל צמד צבע/רקע נבדק. הפלטה ב-§4.1 כבר עברה בדיקה; ערכים חלופיים
  אסורים. `--color-ink-faint` הוא **רצפה** (4.8:1 מול `#f6f9fc`).
- **פוקוס** — טבעת גלובלית `2px` + `outline-offset: 2px`, לבנה על משטחי נייבי
  (SC 1.4.11 דורש 3:1; כחול על נייבי נותן 1.51:1).
- **יעדי מגע** — 44×44 לכל דבר לחיץ, גם כשהמראה קטן יותר (`min-h-11` או הרחבה
  דרך `::before`).
- **טפסים** — תווית קבועה, `aria-invalid`, `aria-describedby`, `role="alert"`
  לשגיאה, `role="status"` להצלחה.
- **מבנה** — `lang="he" dir="rtl"`, skip-link, `<main id="main">`, היררכיית כותרות
  תקינה, `aria-label` לכל אזור ניווט, `aria-current="page"` לפריט פעיל.
- **אזורי גלילה** — `role="region"` + `aria-label` + `tabIndex={0}` לכל קרוסלה,
  וחצי ניווט גם בדסקטופ (אחרת חלק מהתוכן בלתי נגיש בעכבר).
- **תנועה** — `prefers-reduced-motion` מכובד גלובלית; במצב הזה **לא מסתירים כלום**,
  התוכן פשוט מופיע.
- **אייקונים** — `aria-hidden` לדקורטיביים, `aria-label` לפעולות.
- **תמונות** — `alt` אמיתי מה-CMS (`image_alt`); `alt=""` רק לדקורטיבי.

---

## 12. ביצועים ואנימציה

**העיקרון: Progressive enhancement.** קהל היעד מגיע במכשירים איטיים.

```css
/* התוכן מרונדר גלוי. ההסתרה נכנסת רק כש-data-js מסומן על <html>. */
:root[data-js] .reveal:not([data-reveal="in"])  { opacity:0; transform: translateY(30px); }
:root[data-js] .entrance:not([data-reveal="in"]){ opacity:0; transform: translateY(24px); }
```
סקריפט אינליין ב-layout מסמן `data-js`, **עם failsafe שמסיר את הסימון אם ההידרציה
לא הגיעה תוך 4 שניות** — עדיף אתר בלי אנימציה מאתר ריק.

**כללים:**
- **אלמנטי LCP (ה-`h1` בהירו ובכתבה) לעולם לא עטופים באנימציית כניסה.** נמדד:
  ב-6× CPU / 400kbps ה-HTML צויר אחרי 2.28ש׳ אבל כותרת עטופה נשארה שקופה עד 5.95ש׳.
- אנימציית reveal: fade + `translateY(30px)`, easing `cubic-bezier(.22,1,.36,1)`,
  stagger ~90ms בין אחים, IntersectionObserver.
- **אין אנימציית כניסה זהה לכל סקשן.** רגע אחד מכוון (ההירו), והשאר שקט.
- עמודי עיון (FAQ) — בלי reveal בכלל.
- תמונות: `next/image` + `CmsImage`, `priority` רק ל-LCP, `sizes` נכון תמיד.
- פונטים מקומיים woff2 + `font-display: swap`.
- הכל סטטי כברירת מחדל — סינון בצד הלקוח עטוף ב-`Suspense` כדי לא להפוך עמוד
  לרינדור-בכל-בקשה.
- יעד: Core Web Vitals ירוקים במובייל.

---

## 13. סביבה, פריסה ו-CI

```bash
DIRECTUS_URL=https://cms.shelachem.net   # לא מוגדר → תוכן מקומי
DIRECTUS_STATIC_TOKEN=                   # אופציונלי (יש קריאה ציבורית)
REVALIDATE_SECRET=                       # זהה ל-secret ב-Flow של Directus
```

`next.config.ts` מוסיף `images.remotePatterns` ל-`/assets/**` **רק** כש-`DIRECTUS_URL`
מוגדר.

**CI (GitHub Actions) על כל push ל-main ועל כל PR:**
`npm ci` → `npx tsc --noEmit` → `npx eslint .` → `npm run build`, **בלי משתני
סביבה** — כך ה-CI מוודא שהאתר שלם גם על התוכן המקומי.

---

## 14. קופי — כללי כתיבה

- פנייה בגוף שני רבים, עברית מדוברת. ״מגיע לכם״, לא ״הלקוח זכאי״.
- **כותרת = משפט + מילת מפתח מודגשת.** לא ״כותרת שיווקית ארוכה עם שלוש פסיקים״.
- **הרגעה לפני קריאה לפעולה.** לקורא שכבר מתבייש בקביעה נמוכה, ״קיבלתם פחות ממה
  שמגיע לכם?״ בפתיחה נקרא כהאשמה. הניסוח הנכון: ״נבדוק את הקביעה שלכם, ונגיד ביושר.״
- **יושר כמנוע המרה.** ״אם אין בסיס לערר נאמר לכם את זה, בלי לגרור אתכם לתהליך.״
- אין הבטחות שלא נמסרות: אם עמוד מכריז ״המדריך המלא״ — הוא חייב להיות מלא.
- מספרים תמיד `.tnum`, ותמיד עם הקשר.
- מחשבונים והערכות מסומנים במפורש כהערכה.

---

## 15. סדר עבודה מומלץ

1. **תשתית** — Next.js + TS + Tailwind v4, RTL, פונטים, `globals.css` עם כל
   הטוקנים של §4, shadcn/ui, `lib/config.ts`.
2. **Layout** — Header (דסקטופ+מובייל+תפריט), Footer, MobileCtaBar, מודאל ערוצים,
   skip-link, JSON-LD גלובלי.
3. **קומפוננטות משותפות** — `SectionHeading`, `Reveal`/`Entrance`, `Breadcrumb`,
   `NavyHero`, `PageCta`, `LeadForm`, `LeadModal`/`LeadCta`, `FaqAccordion`,
   `Carousel`, כרטיסים, `icons`.
4. **מודל התוכן** — `types.ts` + תוכן מקומי מלא + `lib/content/index.ts` עם ה-fallback.
5. **דף הבית** — 12 הסקשנים לפי §7.1.
6. **תבניות** — עמוד שירות (הכי חשוב מסחרית) → עמוד מוסד → כתבה → אינדקס מגזין.
7. **עמודי משנה** — FAQ, אודות, צוות, רופאים, סניפים, משפטיים, 404/error.
8. **ליד** — `/api/leads`, אטריבוציה, `dataLayer`.
9. **SEO/GEO** — schema, sitemap, robots, llms.txt, canonical לכל עמוד.
10. **Directus** — docker-compose + Caddy, סכמה, הרשאות, seed, מדיה, Flow לרענון,
    `/api/cms-status`, `/api/revalidate`.
11. **QA** — נגישות, ביצועים, RTL, CI ירוק.

**לפני שלב 1 — הצג תוכנית עבודה מפורטת וחכה לאישור.**

---

## 16. קריטריוני קבלה (Definition of Done)

- [ ] `tsc --noEmit`, `eslint`, `next build` — עוברים **בלי משתני סביבה**.
- [ ] כל עמוד נראה ונקרא במלואו כש-JavaScript מכובה.
- [ ] אין `left/right/pl/pr` בקוד — רק מרווחים לוגיים.
- [ ] אין אפור נייטרלי, אין `#0000ff` מרוכך, אין eyebrow מעל כותרת.
- [ ] כל כותרת סקשן עוברת דרך `SectionHeading`; כל מספר-נתון נושא `.tnum`.
- [ ] כל דבר לחיץ ≥44×44; טבעת פוקוס נראית על **כל** רקע, כולל נייבי.
- [ ] כל שדה טופס: תווית קבועה + שגיאה מילולית עם `role="alert"`.
- [ ] כל עמוד: `canonical` + description ייחודי + JSON-LD תקין
      (Rich Results Test עובר, בלי ישויות Organization כפולות).
- [ ] `/sitemap.xml`, `/robots.txt`, `/llms.txt` מיוצרים מהתוכן האמיתי.
- [ ] ליד נשמר ב-Directus; כשל נכתב ללוג עם כל הפרטים; `generate_lead` נורה רק
      אחרי אישור השרת.
- [ ] `MobileCtaBar` מתקפל כשטופס נראה — בכל עמוד שיש בו טופס.
- [ ] אין כתבה מפורסמת בלי גוף; אין קישור שמוביל לעמוד ריק.
- [ ] `prefers-reduced-motion` מכובד; ה-`h1` בהירו ובכתבה מחוץ לכל אנימציה.
- [ ] Lighthouse מובייל: Performance/Accessibility/Best Practices/SEO ירוקים.

---

## 17. נכסים שצריך לקבל לפני התחלה

- פונטים: `RaananPro` (300/400/700/900) ו-`SimplerPro` (300/400/600/700/900) — woff2.
- לוגו: `logo.svg` + `logo-white.svg`; אלמנט הלב הכפול (`heart.svg`) ל-watermark.
- תמונת צוות PNG שקוף להירו (או העלאה ל-`globals ▸ hero_image` ב-CMS).
- `og-default.png` 1200×630.
- מפת ישראל SVG + אייקוני Waze / Google Maps.
- תמונות כתבות, עדויות, רופאים, צוות.
- קבצי העיצוב hi-fi (HTML) — מקור האמת הוויזואלי לכל עמוד.
- נוסח משפטי סופי: מדיניות פרטיות, הצהרת נגישות (כולל פרטי רכז נגישות).

---

## 18. שאלות פתוחות — לענות לפני כתיבת קוד

1. **CMS:** Directus מותקן ורץ? URL, טוקן, גרסה? מי מגדיר collections?
2. **תשתית:** איפה מתארח ה-frontend (Vercel?) ואיפה ה-CMS? מי מנהל DNS ו-SSL?
3. **תוכן:** יש תוכן סופי (רופאים, עדויות, שאלות, כתבות) או מתחילים מ-placeholder?
4. **לידים:** לאן ליד מגיע חוץ מ-Directus — מייל? CRM? וואטסאפ? מי המקבל?
5. **מדידה:** GA4 / GTM / Meta Pixel? מזהים?
6. **נתונים חסרים בגרסה הנוכחית** (מכוונים ל-`null` כדי לא לפרסם מידע שגוי):
   - שעות פעילות אמיתיות לסניפים (`openingHoursSpecification`).
   - קואורדינטות מדויקות לסניפים מ-Google Business Profile (`geo`).
   - כתובות הפרופילים החברתיים האמיתיים (`sameAs`).
7. **רב-לשוניות:** עברית בלבד, או להיערך לערבית/אנגלית ב-CMS מהיום?
8. **טלפון:** האם 076-8017778 הוא המספר הרשמי היחיד? (בעיצובים הופיע גם 072-2582480.)
9. **עדיפויות:** איזה עמוד עולה ראשון לאוויר ומה הדדליין?
