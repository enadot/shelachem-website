# Backlog — אתר שלכם

רשימת שיפורים מתועדפת, מבוססת על סריקת המצב הקיים (יולי 2026). מסמנים `[x]` כשמשימה
הושלמה, ומוסיפים משימות חדשות לסעיף המתאים.

מקרא: **P1** = חשוב עכשיו · **P2** = בהמשך · **P3** = nice-to-have · מאמץ: S קטן / M בינוני / L גדול

➡️ **לסבב UX/UI מעמיק בסשן נפרד:** ראו [סבב UX/UI עומק](#סבב-uxui-עומק--משימות-לסשן-נפרד)
למטה — משימות עם נתיבי קבצים, קריטריוני קבלה וסדר הרצה של פקודות `/impeccable`.

---

## תפעול (Operations)

- [ ] **גיבוי אוטומטי ל-Directus** — P1 · M
  כרגע אין שום גיבוי: כל התוכן והלידים יושבים ב-SQLite + volume של uploads על ה-VPS.
  סקריפט cron יומי שמעתיק את `data.db` (עם `sqlite3 .backup`) ואת `uploads/` ליעד חיצוני
  (S3/Backblaze/rclone לדרייב), כולל נוהל שחזור מתועד ובדיקת שחזור אחת.

- [ ] **התראה על ליד חדש (מייל/וואטסאפ)** — P1 · S
  לידים נשמרים ב-Directus אבל אף אחד לא מקבל התראה — ליד שלא מטופל תוך שעות הולך לאיבוד.
  Flow נוסף ב-Directus: על create ב-`leads` → שליחת מייל לצוות (SMTP ב-docker-compose)
  ו/או webhook לוואטסאפ. אפשר להקים בסקריפט כמו `setup-revalidate-flow.mjs`.

- [ ] **ניטור זמינות + התראות** — P1 · S
  אין שום ניטור לאתר או ל-CMS. UptimeRobot/BetterStack (חינם) על `www.shelachem.net`,
  `cms.shelachem.net/server/health` ו-`/api/revalidate` (תגובת 401 = חי).

- [x] **CI ב-GitHub Actions** — P1 · S
  אין CI — טעויות מתגלות רק ב-deploy. Workflow על כל PR: `tsc --noEmit`, `eslint`, `next build`.

- [ ] **ניטור שגיאות (Sentry)** — P2 · S
  שגיאות ריצה בפרודקשן (למשל כשל שליחת ליד) לא נראות היום בשום מקום. Free tier מספיק.

- [ ] **הגנת ספאם לטופס מעבר ל-honeypot** — P2 · M
  `/api/leads` פתוח ללא rate limiting. להוסיף מגבלת קצב לפי IP (או Cloudflare Turnstile —
  שקוף למשתמש, בלי CAPTCHA מעצבן).

- [ ] **אבטחת CMS** — P2 · S
  2FA לחשבונות אדמין ב-Directus; לשקול Cloudflare (חינם) מול `cms.shelachem.net` — גם CDN
  לתמונות וגם שכבת הגנה; בהמשך אפשר לסגור את הקריאה הציבורית ולעבור ל-static token (מתועד ב-.env.example).

- [ ] **Runbook תפעולי קצר** — P2 · S
  דף אחד ב-`directus/`: מה עושים כשהאתר לא מתעדכן (לבדוק Flow → revalidate), איך צופים
  בלידים ומייצאים אותם, איך משחזרים גיבוי, איך מעדכנים גרסת Directus.

- [ ] **נוהל עדכון Directus** — P3 · S
  ה-image מוצמד ל-`directus/11` (minor נודד). להצמיד לגרסה מלאה, לעדכן ביוזמה אחרי גיבוי.

- [ ] **הוספת `cms.shelachem.net` ל-allowlist של סביבת Claude Code** — P3 · S
  יאפשר תחזוקה, בדיקות חיבור חי והקמת Flows אוטומטית מכאן, בלי להריץ סקריפטים ידנית על השרת.

## UX/UI

- [ ] **העלאת תמונת הצוות להירו** — P1 · S
  הכול מחווט (globals ▸ hero_image); חסרה התמונה עצמה. עד אז החצי הכחול ריק.

- [x] **עמוד 404 ממותג + error boundary** — P1 · S
  אין `not-found.tsx` ו-`error.tsx` — קישור שבור מציג היום עמוד ברירת מחדל חשוף של Next.
  404 עם ניווט חזרה + CTA לבדיקת זכאות.

- [x] **בר CTA דביק במובייל / כפתור וואטסאפ צף** — P1 · M
  אתר לידים חי ומת על נגישות ה-CTA. במובייל, אחרי גלילה מההירו אין דרך מהירה ליצור קשר.
  בר תחתון דביק: חיוג · וואטסאפ · "בדיקת זכאות". לשים לב לא להסתיר תוכן/פוטר.

- [x] **מדידת המרות: עמוד תודה או אירוע** — P1 · S (צד קוד ✓ · נותר להתקין תג)
  אירוע `generate_lead` נדחף ל-`dataLayer` אחרי שמירה מוצלחת בשרת, כולל מקור הליד
  ופרמטרי ה-UTM (`src/lib/analytics.ts`). **נותר תפעולי:** להתקין GTM/GA4 ולמפות את
  האירוע להמרה — בלי תג מותקן האירוע נכתב ל-dataLayer ואף אחד לא מאזין לו.

- [ ] **סקירת נגישות מלאה (WCAG 2.2 AA)** — P1 · M (סריקה אוטומטית ✓ · קורא מסך ✗)
  חובה חוקית בישראל לעסק בסדר גודל כזה. סריקת axe-core (wcag2a/2aa/21aa/22aa) על 6
  עמודים × דסקטופ+מובייל עוברת נקייה, כולל `target-size`; תוקנו ניגודיות `ink-faint`
  וכפתור הוואטסאפ, יעדי מגע בצ'קבוקס ובפוטר, ואזורי גליל נגישים למקלדת.
  **נותר:** בדיקה ידנית עם קורא מסך בעברית (NVDA/VoiceOver) — axe לא מכסה את זה.

- [x] **שיפורי טופס ליד** — P2 · S (UTM ✓ · תוויות קבועות ✓ · נורמליזציית טלפון ✓ · inputmode ✓)
  `inputmode` לטלפון, שמירת פרמטרי UTM מה-URL אל הליד (attribution), גלילה/פוקוס לשדה
  השגוי הראשון, ואינדיקציה על זמן חזרה צפוי ליד הכפתור.

- [x] **Breadcrumbs בעמודי תוכן** — P2 · S
  ה-`BreadcrumbList` נגזר מאותם פירורים שמוצגים ב-`NavyHero`, ולכן כל עמוד משנה
  (מוסדות, שירותים, אודות, צוות, רופאים, סניפים) מקבל אותו בלי כפילות מקורות.

- [ ] **מדדי אמון בדף הבית** — P2 · M (תלוי בחומרים מהלקוח)
  לוגואים "כפי שפורסם ב…", מספרים (לקוחות/שנים/סכומים שהושבו), חוות דעת Google.

- [ ] **חיפוש טקסט חופשי במגזין** — P3 · M
  יש סינון קטגוריות; חיפוש יעזור כשמצטברות עשרות כתבות.

- [x] **Loading UI למגזין** — P3 · S
  `src/app/magazine/loading.tsx` — skeleton בגוני המותג, בלי אנימציית פעימה
  (שלד מהבהב על חיבור חלש נראה כמו תקלה).

## SEO

- [x] **canonical לכל עמוד** — P1 · S
  לא מוגדר בשום עמוד (`alternates.canonical`). חשוב במיוחד למגזין עם `?cat=` — לקבוע
  קנוניקל ל-`/magazine` הנקי.

- [x] **תאריכים אמיתיים לכתבות** — P1 · M
  קיים רק `publishedLabel` טקסטואלי ("יוני 2026"). להוסיף שדה date ב-CMS ובטיפוסים →
  `datePublished`/`dateModified` ב-Article JSON-LD, `lastModified` ב-sitemap, ותצוגה בעמוד.
  בלי זה גוגל מנחש טריות.

- [x] **תמונות OG** — P1 · S
  אין אף תמונת שיתוף — קישור שנשלח בוואטסאפ/פייסבוק מוצג עירום. תמונת ברירת מחדל
  ממותגת (אפשר `opengraph-image` סטטי) + תמונת הכתבה בעמודי מגזין.

- [ ] **קישורי רשתות חברתיות אמיתיים** — P1 · S (בקוד: placeholders כבר מסוננים מה-JSON-LD; נותר לקבל כתובות אמיתיות מהלקוח)
  ב-`src/lib/config.ts` ה-socials הם placeholders (facebook.com בלי עמוד) והם נכנסים
  ל-`sameAs` ב-JSON-LD — מזיק. למלא כתובות אמיתיות או להסיר עד שיהיו.

- [ ] **Google Search Console + Bing Webmaster** — P1 · S (תפעולי)
  אימות בעלות, הגשת sitemap, מעקב שגיאות זחילה והופעות. Bing מזין גם את Copilot (GEO).

- [x] **הרחבת ה-LocalBusiness schema** — P2 · S *(נותרו נתונים מהלקוח)*
  `src/lib/schema.ts` הוא גרף אחד עם `@id` יציבים: `ProfessionalService` ראשי,
  `WebSite`, ושתי ישויות `LocalBusiness` לסניפים עם `parentOrganization`, `hasMap`,
  `areaServed: IL`, `priceRange` ו-`contactPoint`. כל עמוד מצביע לגרף ב-reference
  במקום לשכפל ארגון (קודם כל עמוד הצהיר על ישות משלו).
  **נותר תפעולי:** `openingHours` ב-`config.ts` מכוון ל-`null` ו-`branchGeo` ריק —
  בכוונה, כי שעות/קואורדינטות משוערות גרועות מהיעדרן (גוגל מציג אותן, ומנועי AI
  מצטטים). למלא מה-Google Business Profile וה-JSON-LD ייכלל אוטומטית.

- [x] **Service schema לעמודי שירות** — P2 · S
  `serviceNode()` — `Service` עם `provider`, `areaServed`, `audience` ו-`offers`
  (בדיקה חינם · שכר טרחה רק בהצלחה), לצד ה-`FAQPage` שהיה שם.

- [ ] **אשכולות תוכן וקישורים פנימיים** — P2 · L (מתמשך)
  עמודי עוגן לפי נושא (קצבת נכות כללית, ניידות, החזרי מס, ועדות רפואיות) וכתבות תומכות
  שמקשרות אליהם ולשירותים. זה מנוע הצמיחה האורגני המרכזי לתחום הזה.

- [x] **שדה alt לתמונות תוכן ב-CMS** — P2 · S *(צד קוד ✓ · נותר שדה ב-Directus)*
  `Article.imageAlt` נקרא מ-`image_alt` ב-`withImage()` ומועבר ל-`ArticleImage`.
  **נותר:** להוסיף את השדה `image_alt` ל-collection `articles` ב-Directus.

- [x] **/magazine סטטי** — P3 · S
  הסינון עבר ל-`CategoryFilter` בצד לקוח בתוך `Suspense`; ה-URL נשאר ניתן לשיתוף
  (`?cat=`) והעמוד עבר מ-`ƒ Dynamic` ל-`○ Static` ב-build. נוסף גם מצב ריק לקטגוריה
  בלי כתבות, ו-`loading.tsx` עם skeleton.

## GEO (Generative Engine Optimization — הופעה בתשובות AI)

- [ ] **פרופילים עסקיים ועקביות NAP** — P1 · M (תפעולי-שיווקי)
  Google Business Profile לשני הסניפים + פרופילים באינדקסים ישראליים (דפי זהב, easy וכו')
  עם שם/כתובת/טלפון זהים לאתר. מנועי AI שואבים מהמקורות האלה כשממליצים על ספקים.

- [x] **תבנית "תשובה תחילה" לכתבות** — P2 · S *(תבנית ✓ · תוכן מתמשך)*
  שדה `keyPoints` על `Article` מרונדר כתיבת "בשורה התחתונה" בראש הכתבה — 2–4
  עובדות שעונות ישירות על שאלת החיפוש, לפני הפירוט. מולא לכתבה הקיימת מגוף
  הכתבה עצמה. **נותר:** למלא לכל כתבה חדשה (שדה `key_points` ב-Directus).

- [x] **עמוד FAQ ייעודי** — P2 · M *(מבנה ✓ · הרחבת תוכן נותרה)*
  `/faq` — 21 שאלות בשש קבוצות נושאיות (`src/lib/faq-groups.ts`) עם ניווט דביק
  בעמוד, ה-`FAQPage` JSON-LD היחיד באתר (הוסר מדף הבית כדי שלא יהיו שתי כתובות
  שחולקות אותו תוכן), וקישור מדף הבית, מהפוטר, מהניווט הראשי ומעמודי המוסדות.
  **נותר:** הרחבה לעשרות שאלות בניסוח טבעי ("כמה כסף מקבלים על 40% נכות?") —
  דורש תוכן מקצועי מהלקוח, לא ניסוח מחדש של הקיים.

- [x] **llms.txt** — P3 · S
  `/llms.txt` (route handler, ISR שעה) — מיוצר מהתוכן האמיתי: עובדות על החברה
  (שם משפטי, שנת ייסוד, מודל תשלום, אזור שירות, סניפים), כל המוסדות, השירותים,
  הכתבות ועמוד ה-FAQ. לא מתיישן כשמוסיפים תוכן.

- [x] **היתר מפורש לזחלני AI ב-robots.txt** — P2 · S
  18 סוכנים (GPTBot, OAI-SearchBot, ClaudeBot, Google-Extended, Applebot-Extended,
  PerplexityBot, Bingbot ועוד). `Google-Extended` ו-`Applebot-Extended` מבינים **רק**
  חסימה, ולכן היתר מפורש הוא גם תיעוד וגם הגנה מפני חסימה גורפת עתידית.

- [ ] **מעקב הופעות ב-AI** — P3 · S (נוהל רבעוני)
  לשאול את המנועים המרכזיים (Gemini/ChatGPT/Perplexity/Copilot) שאלות מפתח
  ("חברה למימוש זכויות רפואיות מומלצת") ולתעד אם ואיך שלכם מופיעה, כבנצ'מרק לשיפור.

---

## הושלם

- [x] חיבור Directus מלא עם נפילה חזרה לתוכן מקומי (`src/lib/content`)
- [x] Webhook רענון תוכן: Flow ב-Directus → `/api/revalidate` (סקריפט `run-revalidate-flow.sh`)
- [x] ניהול תמונות דרך ה-CMS + צנרת AVIF/WebP רספונסיבית (Directus assets + `CmsImage`)
- [x] טופס לידים עם ולידציה, honeypot, שמירה ל-Directus והודעת הצלחה
- [x] JSON-LD: LocalBusiness, Article, FAQPage (בית+שירותים), Physician · sitemap · robots
- [x] תיקון גראדיינט ה-BorderBeam שהסתיר את שדה הטלפון בהירו
- [x] הגדרת מחלקת `pill` שחסרה ב-CSS — תגיות וכפתורים חזרו לעיגול המלא שבעיצובים
- [x] מודאל בדיקת זכאות (`LeadModalProvider` + `LeadCta`) — כל ה-CTA-ים פותחים טופס מעוצב במקום קפיצת עוגן
- [x] המתנה לעליית Directus בסקריפטי ההקמה (מונע ECONNREFUSED אחרי restart)
- [x] מיפוי נכסי CMS ל-DIRECTUS_URL + ISR של שעה + `/api/cms-status`
- [x] canonical לכל העמודים · תמונות OG · תאריכי כתבות (schema+sitemap) · 404/error ממותגים · CI · לכידת UTM
- [x] ביקורת עיצוב (impeccable critique, ‎26/40) + תיקונים: ניגודיות AA באדום המותג, קריסת ההירו הריק במובייל, נורמליזציית טלפון, תוויות שדות, בר CTA דביק, skip-link, קופי CTA כן — ‏snapshot ב-.impeccable/critique/
- [x] סבב 2: ‎`/impeccable document` (DESIGN.md + סיידקאר) · ביקורת דו-סוכנית על שלושת עמודי התוכן (20/36) · תיקוני P0 במספרי האמון ובכתבות הריקות · 0 הפרות axe · גרף JSON-LD אחד · `/faq` · `/llms.txt` · מערכת אייקונים
- [x] עמוד המוסד החמישי — משרד הרישוי (`/institutions/misrad-harishui`): הבאנר בדף הבית והפוטר הציגו 5 מוסדות אבל רק 4 היו קיימים


---

# סבב 2 — ביקורת עומק על שלושת עמודי התוכן (יולי 2026)

`/impeccable document` → `DESIGN.md` + `.impeccable/design.json` · `/impeccable critique`
דו-סוכני על `services/[slug]`, `institutions/[slug]`, `magazine/[slug]` (‎20/36).
ה-snapshot המלא: `.impeccable/critique/2026-07-30T*__src-app-services-slug-page-tsx.md`.

## תוקן בסבב הזה

- [x] **P0 · מספרי האמון היו אפס בלי JS, שגויים עם JS, ומסודרים הפוך.**
  `NumberTicker` רינדר `"0"` בשרת — כלומר `/institutions/bituach-leumi` הכריז
  *"0+ תיקים · 0% מהתביעות אושרו · 0% מהעררים"* בכל מצב שבו ה-JS איטי או חסום.
  `useSpring` נעצר אסימפטוטית: נמדד `10,057` במקום `10,059` ו-`4,295+` במקום
  `4,300+`. `inline-block` שבר את הריצה הדו-כיוונית והציג `+4,295`, `%87`, `₪ 0`.
  ו-`prefers-reduced-motion` רק דילג על ההשהיה.
  **תוקן:** הערך האמיתי ב-HTML · `animate` במשך קבוע שנוחת בדיוק על היעד ·
  `<bdi dir="ltr">` · reduced-motion באמת עוצר · ומספר שכבר על המסך בטעינה לא
  נספר (זה היה מייצר הבהוב 10,059 → 0 → 10,059).
  **נמדד אחרי:** `4,300+ · 87% · 62% · 0 ₪` ו-`10,059`.

- [x] **P0 · תשע מעשר כתבות היו עמודים ריקים שהצהירו "המדריך המלא".**
  רק לכתבה אחת יש `body`; השאר רונדרו כותרת + שתי שורות + CTA, עם "12 דק׳ קריאה".
  `isPublished()` בשכבת התוכן: כתבה בלי גוף אין לה URL, אין לה שורה ב-sitemap ואין
  לה כרטיס. המשאבים בעמוד השירות נגזרים עכשיו מהכתבות האמיתיות (גם הכותרת — היא
  כבר סטתה מהכותרת בפועל). **הכתבות עצמן נשארו בתוכן:** ברגע שיתווסף `body` הן
  חוזרות בלי שינוי קוד.

- [x] **P1 · נגישות: שלוש נפילות ניגודיות + טבעת פוקוס בלתי נראית על נייבי.**
  `#d93a38` על `#f6f9fc` 4.31:1 → `#c93330` **4.97:1** · `#c93330` על `#fde3e2`
  4.32:1 → `--color-accent-ink` **5.29:1** · `#d93a38` על `#122680` 2.86:1 →
  `--color-accent-on-navy` **3.84:1** (את השלישית פספסו גם axe וגם ה-detector).
  טבעת הפוקוס הייתה `#0000ff` על נייבי = **1.51:1** מול 3:1 שדורש WCAG 2.2
  SC 1.4.11 → `.surface-navy` מוריש טבעת לבנה (13:1).
  שגיאות הטופס קיבלו `aria-describedby` + `role="alert"` (עד כה ולידציה הייתה
  שקופה לחלוטין לקורא מסך), וצ׳קבוקס ההסכמה מעביר `field.ref` כך ש-`shouldFocusError`
  מגיע אליו — קודם קורא מסך לחץ "שלח", לא שמע כלום, והטופס לא נשלח.
  **נמדד אחרי:** 0 הפרות axe (wcag2a/2aa/21aa/22aa) על 5 עמודים × 2 רזולוציות.

- [x] **P1 · ה-h1 היה מוסתר עד להידרציה.** נמדד ב-6× CPU / 400kbps: ה-HTML צויר
  ב-2.28ש׳ עם 4,045 תווי טקסט, וכל `.entrance`/`.reveal` נשארו ב-`opacity:0` עד
  ש-failsafe ההידרציה שוחרר אותם ב-5.95ש׳ — כמעט 4 שניות של עמוד ריק מול פרסונת
  רבקה, כשהתוכן כבר ב-DOM. ה-h1 הוא גם אלמנט ה-LCP. הוסר `Entrance` מה-h1 בשלושת
  ההירואים.

- [x] **P1 · "לפרטים ←" הבטיח פירוט ומסר טופס בעמוד אחר.** ל-20 מ-23 כרטיסי
  השירות בעמודי המוסדות יש `href: "/#lead-form"`, והקישור היה 350×23px — חצי
  מרצפת המגע. `InstitutionServiceCard`: הכרטיס כולו הוא היעד, התווית אומרת "בדקו
  זכאות", ומודאל בדיקת הזכאות נפתח **עם הנושא ממולא מראש** (`presetTopic`) בלי
  לעזוב את העמוד — קודם המשתמש נזרק לדף הבית ונדרש לזכור על מה קרא ולבחור מחדש
  מרשימה של שבע.

- [x] **P1 · בר ה-CTA במובייל לא התקפל אף פעם.** `MobileCtaBar` מסתתר כש-`#lead-form`
  נראה, אבל ה-id הזה היה רק בהירו של דף הבית — כלומר דווקא בעמודים שנושאים את
  התנועה האורגנית הבר היה קבוע לנצח וכיסה 61px של תוכן. ה-id נוסף לכרטיס הטופס
  בעמוד השירות ולסקשן הטופס בעמוד המוסד. **נמדד:** מתקפל ב-390×844 בשני העמודים.

- [x] **BreadcrumbList נעדר משלושת עמודי התנועה.** רק `NavyHero` פלט את הסכמה;
  עמודי השירות, המגזין והכתבה בנו פירורים ידנית. קומפוננטת `Breadcrumb` אחת
  מציגה **ותמיד** פולטת את הסכמה, בשני גוני משטח.

- [x] **סחף ממערכת העיצוב.** `SectionHeading` (תבנית 300/900 + קו אדום) קיימת
  ושש הכותרות בעמוד השירות שחזרו אותה ידנית ב-`font-bold` בלי הקו — התיקון מחזיר
  את מכשיר הטיפוגרפיה המזהה ביותר של המותג לעמוד עם כוונת הרכישה הגבוהה ביותר.
  שלושה גרדיאנטים כחולים כתובים ידנית עם עצירות שונות (`#0000e6` מול `#0000d6`)
  אוחדו ל-`.brand-gradient`. המחלקה `focus-brand` הופיעה ב-9 קומפוננטות ולא הוגדרה
  באף גיליון סגנון — הוסרה.

- [x] **גרף JSON-LD אחד.** קודם כל עמוד הצהיר על ישות ארגון משלו, כך שגוגל ומנועי
  AI ראו כמה ישויות נפרדות עם אותו שם. `src/lib/schema.ts` — גרף אחד עם `@id`
  יציבים, וכל עמוד מצביע אליו ב-reference.

- [x] **`/faq`, `/llms.txt`, robots ל-18 זחלני AI, sitemap מלא** — ראו סעיפי SEO/GEO.

## נותר — לפי סדר השפעה

- [ ] **אין אף בן אדם בעמודי המוסדות** — P1 · M · *הפער הגדול ביותר שנותר*
  בין ההירו לטופס אין פרצוף אחד, שם אחד, ציטוט אחד או סיפור אחד — רק ארבעה מספרים
  ושמונה קופסאות. מבקר שמגיע מ"ביטוח לאומי ערר" ושוקל למסור תיק רפואי לזרים מקבל
  דיאגרמות תהליך. `TestimonialCard`, `local/testimonials.ts`, `local/team.ts`
  ו-`local/doctors.ts` כולם קיימים ומשמשים בעמוד השירות — ואף אחד מהם לא מופיע שם.
  **קבלה:** לפחות עדות אחת עם שם ותחום, או רופא מהצוות עם פרצוף, מעל הטופס.

- [ ] **תשעה גופי כתבות** — P1 · L (תוכן)
  ה-gate מסתיר אותן, הוא לא כותב אותן. להתחיל מ-`retroactive-tax-refund` — זו
  הכתבה שעמוד הכספים (`/services/tax-exemption`) רוצה לקשר אליה.

- [ ] **מדדי המוסדות הם תבנית עם משתנה אחד** — P2 · S
  ל-4 מ-5 עמודי המוסדות אותם שלושה מדדים (`13` / `10,059` / `0 ₪`) עם מספר אמצעי
  מוחלף, ורק ל-`bituach-leumi` יש `statsNote` שמסביר מאיפה המספרים. או מדדים
  ספציפיים לכל מוסד, או שורת מקור בכולם.

- [ ] **סולם הטיפוגרפיה רחב פי 3.5 ממה שהמערכת מתעדת** — P2 · M
  22 גדלי גופן ליטרליים מול 6 תפקידים ב-`DESIGN.md` (112 ממצאי advisory ב-detector;
  נמדדו 30–34 צעדי טיפוס לעמוד). לאחד לסולם, או להרחיב את `DESIGN.md` במודע.

- [ ] **עמוד השירות הוא שמונה פאנלים זהים בלי דירוג** — P2 · M
  takeaways, זכאות, מצבים, עדויות, FAQ ומשאבים כולם באותו משקל חזותי; רק המחשבון
  הנייבי שובר את רצפת הרעש. שום דבר לא מצהיר על עצמו כחשוב ביותר.

- [ ] **`FaqAccordion` הוא `type="single"`** — P2 · S
  פתיחת תשובה שנייה סוגרת את הראשונה, כך שאי אפשר להשוות "זה פוגע בקצבה?" מול
  "כמה זמן זה לוקח?" — שתי שאלות שנשאלות יחד.

- [ ] **הסיידבר הדביק בעמוד השירות גבוה מהחלון הדביק** — P3 · S
  נמדד 970px מול `top:96px` בחלון 900px, ולכן כפתור "מעדיפים לדבר?" לעולם לא נראה
  כשהמסילה נתקעת.

- [ ] **תגית מעל כותרת (eyebrow)** — P3 · S · *החלטת לקוח*
  `card.tag` מעל `card.name`, ו-"הסיפור שלנו"/"אחריות חברתית" מעל ה-h2 ב-`/about`.
  craft-floor אוסר את זה מוחלטות, אבל אלה אלמנטים בעיצובים המאושרים — לא נמחקו
  חד-צדדית.

- [ ] **בדיקת קורא מסך בעברית** — P1 · S · NVDA/VoiceOver. axe לא מכסה שמות נגישים
  בהקשר, וזה כל מה שנותר מסעיף הנגישות.

---

# סבב UX/UI עומק — משימות לסשן נפרד

הסעיף הזה נכתב כדי להיפתח בסשן חדש ולעבוד ממנו ישירות. כל משימה כוללת **למה**,
**מה לעשות**, **קבצים** ו**קריטריון קבלה**. הבסיס: ביקורת ה-`/impeccable critique`
של דף הבית (26/40) ששמורה ב-`.impeccable/critique/`, ובדיקות דפדפן ב-1440 ו-390.

**לפני שמתחילים בסשן החדש** — להריץ בסדר הזה:
1. `/impeccable document` — מייצר `DESIGN.md` מהקוד (טוקנים, טיפוגרפיה, קומפוננטות).
   כל פקודה אחרת קוראת אותו, אז זה קודם לכל שינוי עיצובי.
2. `/impeccable audit` על האתר — נגישות, ביצועים, רספונסיביות (סוגר גם את סעיף
   הנגישות שהוא חובה חוקית).
3. `/impeccable critique src/app/services/[slug]/page.tsx` וכן על `institutions/[slug]`
   ו-`magazine/[slug]` — שלושת סוגי העמודים שטרם נבדקו ונושאים את התנועה האורגנית.
4. לסיום כל תיקון: `/impeccable polish` (קורא את snapshot ה-critique כ-backlog),
   ואז `/impeccable critique src/app/page.tsx` שוב כדי לראות את הציון עולה.

לא להריץ `bolder` / `overdrive` / `delight`: האתר מוכר אמון לאנשים במצב רפואי —
חגיגת אפקטים תזיק. `live` דורש דפדפן שהמשתמש רואה (לא זמין בסשן מרוחק).

**הערה על impeccable בסביבת Claude Code on the web:** `/plugin` ו-`npx impeccable
install` לא עובדים שם (403 בהורדה). מה שכן עובד — לשכפל את הריפו ולהריץ את ה-skill
ידנית:

```
git clone --depth 1 https://github.com/pbakaus/impeccable.git /tmp/impeccable
# ה-skill: /tmp/impeccable/plugin/skills/impeccable/SKILL.md
# הפלייבוקים: .../reference/{audit,critique,polish,document,…}.md
# ה-detector: node /tmp/impeccable/plugin/skills/impeccable/scripts/detect.mjs --json src
```

הפלייבוק של `critique` דורש שתי הערכות מבודדות (סקירת עיצוב + ראיות detector)
כשני sub-agents נפרדים — אחרת זו ריצה מדורדרת שחייבת לשאת באנר אזהרה.

## P1 — משנה את המספרים

- [ ] **אשף בדיקת זכאות בן 3 שאלות** — P1 · L · *הרעיון בעל ההשפעה הגדולה ביותר*
  **למה:** כל האתר מבטיח "בדיקת זכאות" והבאנר האדום אף מבטיח "כמה שאלות קצרות",
  אבל בפועל מקבלים טופס השארת פרטים. בקטגוריה שכולה אמון, פער בין הבטחה למסירה
  הוא הנזק החמור ביותר — וגם הזדמנות: אשף קצר ממיר טוב יותר מטופס, כי הוא מספק
  ערך לפני שהוא מבקש פרטים.
  **מה:** 3 שאלות (מצב רפואי/תחום · מול איזה מוסד · מצב תעסוקתי), אינדיקטור
  התקדמות, כפתור חזרה, ואחרי השאלה השלישית — הערכה איכותית ("על בסיס מה שסיפרתם,
  סביר שיש בסיס לבדיקה מול X") ורק אז שם+טלפון. התשובות נשמרות על הליד.
  **קבצים:** קומפוננטה חדשה `src/components/shared/eligibility-wizard.tsx`;
  שילוב ב-`lead-modal.tsx` (כמסלול ברירת מחדל) ובהירו; שדה `answers` (json)
  ל-collection `leads` ב-`directus/seed/seed.mjs` + `src/app/api/leads/route.ts`.
  **קבלה:** ניתן לסיים במקלדת בלבד; חזרה שומרת תשובות; ללא JS — נפילה לטופס הרגיל;
  התשובות מופיעות על הליד ב-Directus.

- [ ] **הירו: תמונה, היררכיה ומדדי אמון** — P1 · M
  **למה:** ה-h1 ("13 שנות מקצוענות") מוכר ותק, לא שירות — מבקר ראשון לא מבין מה
  האתר עושה עד שלוש סקשנים מטה. התגית מעל הטופס היא סלוגן, לא הסבר. וחצי ההירו
  הכחול ריק עד שתעלה תמונת הצוות.
  **מה:** להעלות תמונת צוות (CMS ▸ globals ▸ hero_image); כותרת שאומרת מה עושים
  ולמי; מתחת ל-h1 שורת הסבר אחת; ליד הטופס 2–3 מדדי אמון (שנים · לקוחות ·
  "שכר טרחה רק בהצלחה").
  **קבצים:** `src/components/home/hero.tsx`, `src/lib/config.ts` (מספרים).
  **קבלה:** מבקר חדש מבין תוך 5 שניות מה השירות; במובייל ה-CTA מעל הקפל.

- [x] **Reveal — תוכן גלוי כברירת מחדל** — P1 · M
  **נעשה:** `Reveal`/`Entrance` עברו ל-CSS טהור (בלי framer-motion). ההסתרה חלה רק
  תחת `:root[data-js]`, שסקריפט אינליין ב-layout מסמן — ועם failsafe שמסיר אותו אם
  ההידרציה לא הגיעה תוך 4 שניות. **נמדד בדפדפן:** JS מכובה → 14/14 אלמנטים גלויים;
  JS דלוק → 11 מתחת לקפל מוסתרים ונחשפים בגלילה; חסימת כל קבצי ה-JS → גלוי אחרי 4ש׳.
  **למה:** כל סקשן מתחת להירו מרונדר מהשרת עם `opacity:0` ונחשף רק אחרי הידרציה +
  IntersectionObserver. במכשיר איטי (פרסונת רבקה, 58, אנדרואיד ישן) העמוד נראה ריק,
  ואם ה-JS נכשל — ריק לתמיד. זו גם פגיעה ב-SEO/GEO וגם באמון.
  **מה:** להפוך את `Reveal` ל-progressive enhancement: מרונדר גלוי, והאנימציה
  מתווספת רק אחרי הידרציה (או ב-CSS טהור עם `@starting-style` / `animation-timeline`).
  **קבצים:** `src/components/shared/reveal.tsx` (וכל 20 השימושים מרוויחים אוטומטית).
  **קבלה:** עם JS מכובה בדפדפן — כל התוכן נראה; עם JS — האנימציה כמו היום.

- [x] **14 צ'יפי ההתמחויות — להפוך לניווט אמיתי** — P1 · M
  **נעשה:** 14 התחומים קובצו ל-5 כרטיסי מוסד, כל כרטיס קישור אמיתי לעמוד המוסד.
  הצ'יפים נשארו כפירוט בתוך הכרטיס, בלי hover ובלי `cursor-default` מטעה.
  **נמדד:** 5 בחירות ברמה הראשונה (≤6 ✓), 14 צ'יפי פירוט, 0 אלמנטים עם hover שאינם קישור.
  **למה:** 14 אפשרויות בנקודת החלטה אחת (מעל גבול 4 של זיכרון עבודה), והם נראים
  קליקביליים (`hover:border-brand`) אך אינם קישורים (`cursor-default`) — הבטחה
  ויזואלית שנשברת.
  **מה:** או לקשר כל צ'יפ לעמוד השירות/המוסד המתאים, או לקבץ ל-4–6 קטגוריות עם
  פירוט מתקפל. בכל מקרה — להסיר hover ממה שאינו לחיץ.
  **קבצים:** `src/components/home/specialties.tsx`.
  **קבלה:** אין אלמנט עם hover שאינו אינטראקטיבי; ≤6 בחירות ברמה הראשונה.

- [ ] **נגישות WCAG 2.2 AA — סבב מלא** *(חובה חוקית בישראל)* — P1 · M · **כמעט**
  **תוקן ונמדד:** צ'קבוקס — יעד מגע 44×44 דרך `::before` בלי לשנות את המראה; קישורי
  פוטר — `min-h-11` (נמדד 44px); אזורי גליל (עדויות בדף הבית) — `role=region`+שם+
  `tabIndex`; ניגודיות — `--color-ink-faint` ל-#616f88, ירוק הוואטסאפ ל-#15793a,
  ותווית המחשבון על האדום ללבן מלא. סריקת axe על 6 עמודים × 2 רזולוציות: 0 הפרות,
  כולל כלל `target-size` של WCAG 2.2.
  **התברר כלא-בעיה:** הגליף "+" ב-FAQ כבר `aria-hidden`; העברת הפוקוס לשדה השגוי
  הראשון כבר עובדת (ברירת המחדל של react-hook-form) — שניהם נבדקו בדפדפן.
  **נותר:** בדיקה ידנית עם קורא מסך בעברית — axe לא מכסה שמות נגישים בהקשר.

- [x] **מדידת המרות** — P1 · S (אירוע ✓ · תג מדידה ✗)
  `generate_lead` ל-`dataLayer` אחרי תשובת 200 מהשרת בלבד (לא נספרות המרות שנכשלו),
  עם `lead_source` הכולל UTM. נבדק מקצה לקצה בדפדפן. נותר להתקין GTM/GA4.

## P2 — חיזוק המסע

- [x] **הדר דביק (דסקטופ)** — P2 · M
  `<header className="sticky top-0 z-30">` — גם landmark `<header>` שלא היה קיים
  כלל (היו רק `<nav>`). קומפקטי (לוגו 44px, `py-3`) כדי שהנוכחות הקבועה לא תגזול
  גובה, בלי מאזין scroll ב-JS. הניווט גם נדלק נכון ב-`/services/*` (עמוד-בן של
  `/institutions`) — קודם שום פריט לא נדלק שם.

- [x] **כפתור הטלפון בהדר — חיוג ישיר במובייל** — P2 · S
  במובייל העיגול הוא `<a href="tel:">` ישיר; בדסקטופ המודאל נשאר.

- [ ] **מדדי אמון בדף הבית** — P2 · M *(תלוי בחומרים מהלקוח)*
  מספרים אמיתיים (לקוחות/שנים/סכומים שהושבו), חוות דעת Google, לוגואי "פורסם ב…".
  העדויות היום אנונימיות (ראשי תיבות, בלי סכומים) — הוכחה חלשה מול "10,059 לקוחות".

- [ ] **סדר הסקשנים בדף הבית** — P2 · M
  10 סקשני h2 ברצף. לבחון העלאת ההוכחה החברתית (עדויות/מספרים) לפני "איך זה עובד",
  ולקצר את הדרך בין ההבטחה להוכחה. שווה A/B אם יש תנועה.

- [x] **placeholder הכתבות** — P2 · S *(תוקן · העלאת תמונות אמיתיות נותרה)*
  `article-placeholder.avif` הוא 296×222 והוגש בקופסה של 944×420 (מתיחה 3.3×)
  ובכרטיסים במתיחה 4.6× — והוא היה **אלמנט ה-LCP** של עמוד הכתבה, בארבעה עותקים
  בכל עמוד. הוחלף ב-`ArticleImage`: תמונה אמיתית מה-CMS מוצגת כרגיל עם ה-alt שלה,
  ובהיעדרה משטח מותג עם סימן הלב (~1KB, לא מתחזה לצילום).
  **נותר:** להעלות תמונה אמיתית לכל כתבה.

- [ ] **מיקרו-אינטראקציות מכוונות במקום אנימציה אחידה** — P2 · S
  אותה כניסת `Reveal` על כל סקשן = דקדוק שלא נבחר (craft-floor). לבחור רגע אחד
  מחושב (למשל חשיפת הטופס או מעבר שלבי האשף) ולהשקיע בו, ולהשקיט את השאר.

- [x] **Breadcrumbs בעמודי תוכן** — P2 · S
  `BreadcrumbList` נוסף ב-`NavyHero` עצמו, ולכן חל על כל עמודי המשנה בבת אחת.

- [x] **עקביות מיקרו-קופי וחיצים** — P2 · S
  **תוקן במלואו.** נוצרה מערכת אייקונים (`src/components/shared/icons.tsx`): SVG
  מצוירים במשקל קו אחיד שמחליפים 35 גליפי יוניקוד — 10× `‹`, 15× `›`, 10× `←`
  בשלוש קונבנציות שונות לאותה משמעות, וכן `✓` ו-`💡` שתפקדו כאייקונים. גליף בתוך
  טקסט של כפתור גם נקרא בקורא מסך ("סימן ציטוט זוויתי"). הכיוון מפורש (`dir="rtl"`
  ⇒ קדימה = שמאלה) ולא נשען על היפוך דו-כיווני.
  הפוטר: "סניפים" עבר לעמודת "אודות", עמודת "מגזין" הפכה ל"ידע ותשובות" עם ארבעה
  יעדים שונים (קודם "כל הכתבות" ו"מדריכים" הצביעו לאותו מקום), ו"מחלות וזכויות"
  מצביעה לעמוד שמטפל בכל זכות במקום ל-`/institutions` הגנרי.

## P3 — ליטוש

- [ ] **`loading.tsx` עם skeleton** — P3 · S · למגזין (העמוד הדינמי היחיד).
- [ ] **חיפוש טקסט חופשי במגזין** — P3 · M · כשיצטברו עשרות כתבות.
- [ ] **מצב שגיאה עשיר בטופס** — P3 · S · backoff בניסיון חוזר, שמירת טיוטה ב-localStorage
  כדי שהפסקה במובייל לא תמחק את מה שהוקלד.
